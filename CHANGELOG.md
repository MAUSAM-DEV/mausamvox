# Changelog

One dated line per completed step/session. Newest first. Each entry ends with the commit hash.

## 2026-07-13

- Design-system polish Phase 1 — global contrast/vibrancy token pass, purely visual, zero logic/layout/copy changes (942 lines across 59 files, all mechanical color substitutions). Root finding: the Tailwind color tokens in `tailwind.config.ts` are defined but almost never used as classes — components inline the raw hexes (~1,200 occurrences), so the fix was one old→new mapping applied uniformly repo-wide (src `*.ts/*.tsx/*.css` + tailwind.config.ts) PLUS the config/globals updated so the canonical tokens match. **Contrast (WCAG, verified by script against the lightest common surface #13132A):** secondary-text grays raised from 2.8–4.3:1 to 5.8–7.9:1 — `#5A5A80→#8E8EB4` (main muted, 156 uses), `#606088→#9494BC` (token `muted`), `#7878A0→#A0A0C8`, `#8888AA→#A8A8CC`, `#6E6E98→#9A9AC0`, placeholder-only `#4A4A6A→#6E6E96`; borders crisper: `#1E1E3A→#2E2E56` (1.18→1.49 vs page bg), `#2A2A4A→#3C3C6A`, `#272745→#383866`, `#3A3A60→#4A4A7A`. **Vibrancy:** violet `#8B5CF6→#9D5CFF`, pink `#EC4899→#F9459E`, cyan `#0CC7E8` (accent rgba() variants remapped to match: 139,92,246→157,92,255 · 236,72,153→249,69,158 · 6,182,212→12,199,232 · 90,90,128→142,142,180); brand gradients + scrollbar + range-slider in globals.css updated. Unchanged by design: background layers (void/deep/surface/card), primary `#F0F0FF` + bright-secondary `#C4C4E0` (already 10.7–18:1), semantic status colors (red/amber/green), favicon assets (svg/png/ico left as a consistent set). Noted, not restructured (Phase 2 candidates): components bypass Tailwind's spacing/type tokens with inline styles throughout; sung-line dim `#2B2B45`/opacity-0.4 states in LyricsPane are intentional "spent" styling and were left alone. tsc clean; clean `npm ci && npm run build` passes. ⚠️ Visual regression check on live is the acceptance: skim dashboard / voice-swap result / Perform Live / library on a phone — text should read clearly, nothing should look washed-out or neon. (commit: 1fe1867)
- Share as Video — free branded vertical (9:16) MP4 export of a saved track for Instagram Reels / YouTube Shorts / WhatsApp / TikTok; server-side ffmpeg only (bundled binary — no model, no API cost, no credit charge, growth feature). New `POST /api/share-video`: takes `{swapId}` (session auth + ownership check) or `{token}` (valid public share token), signs the track's `result_path` FRESH (sign-on-read, never a stored URL), and renders 1080×1920@30fps H.264 (veryfast, CRF 23) + AAC 160k + faststart with an animated brand-gradient background (`gradients` filter, purple→near-black), a `showwaves` cline waveform of the actual audio (purple/pink), and three `drawtext` overlays — track title, voice line, "Made with MausamVox" — with user strings passed via `textfile=` + `expansion=none` so song names with quotes/colons/percent can't break the filter. Clip capped at 25 s (`CLIP_SECONDS` constant): a full-length render of a 4-min track would risk the 60 s function budget, so the video is deliberately a teaser — and render time is logged (`[share-video] render ms=`) for tuning. **Measured locally on the exact bundled ffmpeg-static binary: 25 s @ 1080×1920 renders in ~4 s (~2.6 MB), 720×1280 in ~1 s — full Reels resolution fits the budget even at 5-10× lambda slowdown, so no 720p fallback needed (drop `VIDEO_W/H` if live numbers say otherwise).** A frame render was visually verified (layout/colors/text correct). drawtext needs a real font file: bundled `assets/fonts/NotoSans-Regular.ttf` (27 KB latin subset, SIL OFL 1.1) copied from next's own `@vercel/og` compiled assets — zero downloads, provenance + license documented in `assets/fonts/README.md`; the TTF + ffmpeg binary ship via `outputFileTracingIncludes['/api/share-video']`. Rendered on demand and returned as bytes — nothing stored, no DB table, no migration. UI: new shared `ShareVideoButton` ("🎬 Share as Video", disabled until the persist lands — ShareControl's pattern) next to Share on BOTH the swap Result screen and `/swaps/[id]`; click → "Rendering video…" → modal with a 9:16 `<video>` preview, "⬇ Download MP4", and — where `navigator.canShare({files})` is supported (phones) — "📤 Share…" opening the system sheet straight into Reels/WhatsApp (share-sheet cancel is not treated as an error). Render failures toast clearly and affect nothing else. tsc clean; clean-install build passes. ⚠️ Untested on live — acceptance = one render (report the logged ms), phone share-sheet run, and a QuickTime/gallery playback check of the downloaded file. (commit: 1f2527e)
- ElevenLabs Music engine for Song Studio (swappable, ACE-Step fallback) — `SONG_ENGINE` env selector added to `src/lib/song-engine.ts` mirroring rvc-engine.ts: `elevenlabs` (default, no dashboard step to adopt) / `acestep` (instant rollback; the entire ACE-Step create+poll path is untouched behind the flag, including its loudnorm pass and the GET poll handler — which also still serves any prediction in flight across a flip). New `src/lib/song-engine-elevenlabs.ts` (docs consulted live 2026-07-13): endpoint `POST https://api.elevenlabs.io/v1/music?output_format=mp3_44100_192`, auth `xi-api-key: process.env.ELEVENLABS_API_KEY` (never hardcoded), body `{ model_id: 'music_v2', composition_plan: { chunks } }`. Mapping: exact user-written lyrics require a composition plan (the simple `prompt` param is describe-a-song mode and mutually exclusive with plans); Song Studio's existing `[verse]/[chorus]`-tagged lyrics convention maps almost directly onto music_v2 chunk text (`[Section]` labels + newline lines + `{curly}` directions) — `buildCompositionPlan()` splits on `[tag]` lines, distributes the requested duration across sections weighted by line count (clamped to the API's 3–120 s/chunk, ≤30 chunks, overflow folded into the last chunk), passes the comma-split style prompt as every chunk's `positive_styles` (≤50), and turns a bare `[instrumental]` into an `{instrumental}` direction. The API returns mp3 BYTES synchronously (no URL, no polling) — so on this engine POST does the whole job: config gate BEFORE the charge (misconfigured key costs nothing) → atomic `deduct_credits` (admin bypass) → compose with a 45 s AbortController (route maxDuration 60; a timeout refunds and is retryable) → upload to the voice-swaps bucket → `kind='song_studio'` row with `replicate_prediction_id='el-<swapId>'` (satisfies the unique index, can't collide with Replicate ids) → returns `{status:'succeeded', swapId, url}`; SongStudioPage short-circuits its poll loop when POST already carries the finished song (acestep clients still poll GET exactly as before; POST now also takes `title` for the synchronous persist). ANY post-charge failure refunds via the POST catch (`refunded: true` in the response) — never charged on failure. Loudness: deliberately NO loudnorm on ElevenLabs output (it ships mastered; double-compression risk) — `normalizeLoudness` stays in place for the ACE-Step path. `SONG_STUDIO_CREDITS` left at 50 with a comment that it must cover ElevenLabs cost + margin (founder's final number pending). `.env.local.example` documents `ELEVENLABS_API_KEY` + `SONG_ENGINE`. tsc clean; clean-install build passes. ⚠️ Untested on live — needs `ELEVENLABS_API_KEY` set in Vercel (Music API requires a paid ElevenLabs plan); acceptance = one generation (single-request, listen for mastering), one forced post-charge failure (balance restored), one `SONG_ENGINE=acestep` rollback run. (commit: 6413082)
- Optional ffmpeg vocal cleanup for clone-training input — cleaner training samples = better clones, ffmpeg-only (no model, no API cost, no credit charge). New shared `src/lib/denoise.ts` (loudness.ts pattern: temp-file I/O because m4a can't stream via stdin, 45 s timeout, NEVER throws — any ffmpeg failure returns the ORIGINAL buffer so the pipeline proceeds as if cleanup was off): chain = `highpass=f=80` (rumble/mains-hum below male vocal fundamentals ~85 Hz) → `afftdn=nr=12:nf=-50:tn=1` (FFT denoiser at ffmpeg's own conservative default strength, adaptive noise tracking for non-stationary phone-recording noise), output mono 44.1 kHz s16 WAV with `-bitexact` (minimal header, same shape as prepare-dataset's convertToWav) — every setting a named constant (`DENOISE_HIGHPASS_HZ` / `DENOISE_STRENGTH_DB` / `DENOISE_FLOOR_DB`) for easy tuning. The exact filter chain was verified against the bundled ffmpeg-static binary locally (valid RIFF output, duration preserved). Applied in `/api/prepare-dataset` BEFORE the split-into-clips step via new body flag `denoise` (default true server-side): cleaned WAV feeds the native splitWav path; cleanup failure or toggle-off = the existing WAV/mp3/m4a/webm handling byte-for-byte — clip logic, ZIP packaging, upload, training untouched. Voice Lab UI: "Clean up background noise *(recommended)*" checkbox in the step-2 action bar (default ON, tooltip explains; turn off for an already-clean studio recording), threaded through `useStudioTraining` start/retry and the My-Voices open-voice path. ffmpeg binary already traced for this route since `c250c7e` — no config change. Not applied to Choir/Instruments inputs (deliberate no-over-scope; the helper is shared and ready if wanted). tsc clean; clean-install build passes. ⚠️ Untested on live — acceptance = train one clone from a noisy phone recording and LISTEN for watery artifacts (tune `DENOISE_STRENGTH_DB` down if present); grep Vercel logs for `[prepare-dataset] denoise applied`. (commit: d3adeb8)
- Recording-quality wizard (noise/clip metering in the mic recorder) — capture-time quality aid for the in-browser recorders, front-end only (no backend, no model, no per-run cost); what gets recorded/uploaded is byte-identical (same getUserMedia constraints, same MediaRecorder / ScriptProcessor paths — the wizard only decides WHEN capture starts, so clone/swap inputs get quieter rooms and unclipped takes at the source). Investigation first: there was NO shared recorder — five independent implementations (Voice Lab Quick + Pro, Choir, Instruments, Karaoke) sharing only low-level helpers (`audioUtils.ts`) — so the wizard was factored ONCE into new `src/components/recording/`: `micMeter.ts` (`MicMeter`: one AnalyserNode per open stream; `getFloatTimeDomainData` with byte fallback for older Safari; AudioContext created in the tap's call chain + explicit `resume()` for the iOS suspended-context quirk; energy-averaged noise-floor measurement; all thresholds tunable constants — ambient > −38 dBFS = noisy, < −48 = quiet, peak ≥ 0.985 = clipping, 4× rolling-average = spike) and `MicCheckWizard.tsx` (wizard: trigger button → permission ask with "tap Allow" copy and inline retry on deny → ~1.6 s "stay quiet" room check with live meter → quiet: straight to a 3-2-1 countdown; noisy: "Too noisy — quieter room = clearly better recording" with Check again / Record anyway / Cancel → hands the ALREADY-OPEN stream + meter to the page's existing recorder via ownership transfer, so cancelling/unmounting can never kill a live take; plus `RecordingQualityMonitor`: during-recording level bar, "🔴 Too loud — move back" clip warning near 0 dBFS with 2 s hold, and a non-destructive "⚡ loud background noise" spike warning with 4 s hold — warn only, the take always continues). Wired into all four standalone recorders: Quick Record (its waveform now reads the meter's analyser — duplicate AudioContext deleted), Pro Record (its RMS bar replaced by the shared monitor; device picker → wizard → same 48 kHz WAV graph), Choir and Instruments (record button swapped for the wizard trigger, monitor shown while recording, meters closed everywhere tracks are stopped incl. unmount). KaraokePanel deliberately untouched — its recording is offset-synced to backing playback and a pre-roll would skew the duet-mix alignment (logged as a possible v2 with its own timing handling). No migration, no new routes. tsc clean; clean-install build passes. ⚠️ Untested on live — the acceptance pass should be on a PHONE (Chrome Android + Safari iOS), incl. deny-then-allow permission, noisy-room verdict buttons, mid-take clap spike, and iOS meters actually moving. (commit: 10621c7)
- Guided AI Cover flow (zero-setup covers via Library voices) — overlap with Voice Swap was assessed FIRST, per the task: Voice Swap already IS a 3-step wizard with Library-voice support (server-side model resolution + `?libVoice=` deep link, from `eaba2d6`), so a parallel page/pipeline would have been redundant and was NOT built. What was genuinely missing: (a) the picker's Library tab only ever showed a deep-linked voice — it never fetched the community list, so it sat EMPTY for anyone arriving directly, making zero-setup covers impossible from inside the flow; (b) no first-run framing for users who haven't trained a voice. Shipped as a thin guided mode over the SAME pipeline: `VoiceSwapPage` gained a `guided` prop and now loads the full published list via GET /api/library into the picker (fixes the empty Library tab on normal /voice-swap too; own clones win duplicate ids across the step-2 refetch merge, which previously DROPPED merged library voices). New `/ai-cover` route renders `<VoiceSwapPage guided />`: Library tab first with the first community voice preselected (never overrides a user choice or `?libVoice=`), duet toggle + duet-split fallback hidden (UploadStep `guided`), Swap Controls hidden behind their defaults with an honest "smart defaults applied — pitch auto-matched" note linking to /voice-swap for full control (ConfigStep `guided`), breadcrumb relabeled Pick a song / Choose a voice / Your cover (VTopbar `labels`), guided banner, "🎤 Generate My Cover" button. Same credits (200 full swap / preview gate / 50 stem-split — no new charges), same ResultStep (player + Download + Share + polish), same persist → Recent/Saved Tracks. Wired prominently: dashboard card (first, ✨ "no training needed" copy), Tools list in both sidebars, `/ai-cover` added to middleware gate + matcher. No migration, no new API routes, no DB changes; a still-empty library (migration `20260713000000` unapplied or nothing published) degrades honestly to the Browse Library card. tsc clean; clean-install build passes (`/ai-cover` static, prerendered). ⚠️ Untested on live. (commit: c1b793e)
- AI lyrics generator feeding Song Studio — "✨ Write lyrics with AI" toggle above the lyrics box opens an inline panel (theme required; language = English / Hindi-Devanagari / Hinglish / Punjabi-Roman / Tamil / Bengali; optional mood + structure preset) and fills the editable textarea with `[verse]/[chorus]/[bridge]`-tagged lyrics in exactly the format ACE-Step expects. Engine: **openai/gpt-4o-mini on Replicate** (`src/lib/lyrics-gen.ts`, version pinned `86d7f12d…`, reuses REPLICATE_API_TOKEN — no new provider). Verified live 2026-07-13: by-version create works, format adherence exact, natural Hinglish; per-run cost ≈ **$0.0004** (~300 in + ~600 out tokens at $0.15/$0.60 per M — Likely, unverified: exact rates). anthropic/claude-3.5-haiku was evaluated first and REJECTED: its Replicate backend 500'd on every request incl. a bare "say hello" (eval spend today ~$0.001 total across test runs). New synchronous `POST /api/lyrics-gen` (maxDuration 60, LLM answers in ~3-8s): `LYRICS_GEN_CREDITS = 5`, atomic `deduct_credits()` up front + `add_credits()` refund on EVERY failure after the charge (create/poll/timeout/empty-output), ADMIN_EMAILS bypass — Choir/Instruments pattern; nothing stored server-side, the user owns the draft in the textarea. Belt-and-braces sanitizer cuts any pre-tag preamble, markdown fences and trailing spaces, caps at the textarea's 5000 chars. Honest copy in-panel: replaces the box contents; AI lyrics are a starting point that may need editing and must not copy existing songs. No migration, no new grants (deduct/add RPCs already exist). tsc clean; clean-install build passes (`/api/lyrics-gen` dynamic). (commit: f5e5ad9)
- Lyrics transcription accuracy: engine switched to **victor-upmeet/whisperx** (`655845d6`, pinned) from vaibhavs10/incredibly-fast-whisper (`3ab86df6`). Investigation first confirmed the pipeline ALREADY transcribes the isolated vocal stem everywhere (Stem Studio passes `stems.vocalsPath`, saved swaps `swap.vocal_stem_path` — original pre-RVC vocal), so per the task the fix targeted the engine instead: the old cog's HF *chunked* pipeline + native cross-attention word timestamps were the real problem. Verified by a paid A/B on a real stored Hindi stem (`stems/x0pczbb6…-vocals.mp3`, ~$0.03 total eval spend on 2 runs + 1 old-row comparison): old stored row had words stretched across 27-SECOND spans, a U+FFFD replacement char, fragmented 1-word lines and mangled text; WhisperX returned 12 coherent VAD segments, 193 aligned words (max word span 3.88s = a held note), sub-second timing. Mechanisms: VAD pre-segmentation feeds Whisper only actual singing (silence/instrumental bleed — the hallucination trigger — is skipped) and wav2vec2 forced alignment pins each word to audio. Same Whisper large-v3 underneath; per-run cost ballpark unchanged (predict_time 10-17s vs similar before; no added Replicate call — Likely, unverified: exact per-second GPU rates). Integration kept every downstream feature byte-compatible: same WordTiming/LyricLine shapes, same flatten→romanize→hum-filter→groupWordsIntoLines pipeline (VAD segment boundaries naturally re-emerge via the 0.8s-gap heuristic), regenerate/edit/cache/25cr-charge-after-store untouched; language map now ISO codes (auto omits the param → auto-detect). Unaligned tokens (digits/symbols) anchor to the previous word's end instead of being dropped; a language with no wav2vec2 aligner (only reachable via auto-detect; en/hi both have one) falls back to segment-level lines, which LyricsPane already renders as whole-line highlight (the edited-row path). Parser verified by running the exact mirrored logic against the real prediction output (39 clean display lines, 0 pathological spans). Old stored rows untouched — a 25cr Regenerate re-runs a track on the new engine. tsc clean; clean-install build passes. (commit: e987979)
- Voice Library — browse & use community-shared voices (free, no-payment MVP; paid licensing + creator payouts explicitly "coming later" in the copy, never faked). **⚠️ needs migration `20260713000000` (NOT applied to prod)**: adds `published` (default false), `published_at`, `library_consent_at`, `library_bio` to voice_clones + a partial index; **NO new grants** — every library DB op runs through service-role API routes whose SELECT/UPDATE on voice_clones is already exercised in prod (sample-url, train), and new columns ride existing table-level grants; no anon grant because unauthenticated browsing never touches PostgREST. **Publishing** (share_token's opt-in pattern): 🌐 button on ready voices in Voice Lab's My Voices panel → inline panel with REQUIRED consent checkbox ("I own this voice or have the rights, and I consent to others using it in the Library"), enforced server-side too in POST /api/library/publish (403 not-owner, 409 not-ready, consent 400); library_consent_at records the consent event and is KEPT on unpublish (audit trail; `published` alone controls visibility). **Browse**: public /library page (NOT middleware-gated — browsing needs no auth), search + language filter, sample preview via GET /api/library/preview (sign-on-read 307, published rows only, private voices 404 identically), only publish-safe fields exposed (never user_id/model/sample paths). **Use** (login required — /voice-swap stays gated): "Use in Voice Swap →" passes ?libVoice=<id>; the picker loads it as a 🌐 "Community voice" under the previously-cosmetic Library tab (My Voices/Library tabs now actually filter; Ghost Singers unchanged), preselects it, and /api/voice-convert + sample-url gained a published-voice fallback after the ownership query misses (auto key-match works for library voices; model still resolved server-side by voiceId — model URLs never reach the browser). Deploy-outruns-migration safe everywhere: missing published column reads as empty library/not-found, publish POSTs get an honest 503. Wired: Voice Library link in both sidebars + dashboard tool card; Marketplace stub stays dim+Soon. tsc clean; clean-install build passes (/library static, 3 dynamic API routes). (commit: eaba2d6)
- Real Account Settings page — the "Settings · Soon" stub in both avatar dropdowns (dashboard header + landing nav) is now a working `/settings` page (`src/components/settings/SettingsPage.tsx`, `/swaps`-shell styling), middleware-gated like the other app pages. Only controls with a real backend today, per the honesty rules: **Display name** (updates `public.users.full_name` via the existing RLS update-own policy + authenticated grant, AND auth `user_metadata.full_name` since the dashboard/nav display the metadata name); **Email change** (`auth.updateUser({ email })` with the confirmation link routed through the existing `/auth/callback?next=/settings`; UI states the change applies only after confirming — Supabase's secure-email-change default sends links to both addresses); **Password change** (inline form reusing `PasswordInput` + the same signed-in `auth.updateUser({ password })` call as `/auth/update-password`; the `au-*` input styles are replicated in the page's style block because they otherwise live only in the auth layout); **Account info** read-only (plan, credits_remaining, member-since from the users row); **Danger zone** = Sign Out only — account deletion deliberately ABSENT (no safe deletion flow exists: no storage cleanup or admin-delete route, and no support channel to link), stated honestly in one line instead of a dead control. No migration needed — every query rides existing grants/policies. Verified: tsc clean, clean-install build passes (`/settings` static, 4.71 kB). (commit: 7515831)

## 2026-07-12

- Loudness-normalize ALL generated audio before storage — Song Studio output (ACE-Step comes back much quieter than the rest of the app), Choir stacks (amix attenuates the sum), and Instruments renders now play at a consistent level. New shared helper `src/lib/loudness.ts`: EBU R128 `loudnorm` targeting **−14 LUFS integrated / −1 dBTP / LRA 11** (the three targets are exported constants — one place to tune), proper TWO-PASS operation (pass 1 measures and prints JSON stats, pass 2 applies a linear non-pumping gain with the measured values; parse failure such as silence/−inf falls back to single-pass dynamic), output pinned back to 44.1 kHz (loudnorm upsamples to 192 kHz internally), mp3 (256k) or wav preserved per input. The helper NEVER throws: any ffmpeg failure returns the original buffer, so a quiet track is stored rather than failing a generation the user already paid for. Applied in all three routes after generation/download and before the voice-swaps upload; Instruments keeps its +12 dB/limiter encode as the fallback level. `/api/song-studio` added to next.config outputFileTracingIncludes (ffmpeg binary — without it the lambda would silently fall back every time). Verified: two-pass flow proven with the bundled binary (−47 LUFS test tone → measured −14.05 LUFS out), tsc clean, and a full clean-install build (`rm -rf node_modules .next && npm ci && npm run build`) passes with all routes intact. (commit: 26b0da0)
- Fix the Vercel build failure that kept production stuck on an old commit: `/api/instruments` crashed page-data collection with "Cannot destructure property 'Midi' of … undefined" because the heavy audio deps were imported at module scope and evaluated at BUILD time, where @tonejs/midi's ESM/CJS interop yields undefined — and `serverComponentsExternalPackages` demonstrably does NOT keep packages out of route-handler bundles in Next 14.2 (webpack chunk ids in the crash stack). Reproduced first the way Vercel builds (rm -rf node_modules .next && npm ci && npm run build — npm ci itself passed, so the lockfile/overrides were already deterministic). Fix: all five heavy deps (@tensorflow/tfjs, tfjs-backend-wasm, @spotify/basic-pitch, @tonejs/midi, js-synthesizer) now load lazily inside the request path — dynamic import() with an explicit default-vs-named interop guard (tonejs's Midi resolved from either shape, hard error if neither), js-synthesizer via createRequire; module-level singleton promises preserve warm-lambda reuse; route gained `export const runtime='nodejs'` + `dynamic='force-dynamic'`; a header comment bans future top-level heavy imports in this file. @tonejs/midi promoted to an explicit package.json dependency (^2.0.28, was transitive-only). VERIFIED on a clean install: full `npm ci && npm run build` passes twice, `/api/instruments` listed as a dynamic route alongside all others (choir/song-studio/voice-convert/credits unchanged), and the tonejs interop path proven at runtime (Midi class resolves, writes MIDI bytes). tsc clean. (commit: ac318a5)
- Swap the Instruments soundfont for licensing: `TimGM6mb.sf2` (GPLv2 — a poor fit for a bundled asset in a commercial product) replaced with **GeneralUser GS v2.0.3** (`assets/soundfonts/GeneralUserGS.sf2`, 32.3 MB, from the author's official GitHub). License verified from the repo's LICENSE.txt and quoted verbatim in assets/soundfonts/README.md: use "without restriction… private or commercial", explicit software-project bundling + modification permission, samples "allow full use in music production, including the ability to make profit", and distributors are asked to ship a local copy (exactly what we do); the author's sample-origin caveat is quoted too. All 32 exposed GM programs re-verified present in bank 0 by parsing the sf2's phdr preset headers (names match: Violin/Sitar/Shenai/Steel Drums/…) — no remapping needed. Every reference updated (instruments route path, next.config outputFileTracingIncludes, instruments.ts comments, spike-script header). Pipeline re-proven locally with the new font on Grand Piano + Violin (renders 22-23 ms, peaks −11/−9.7 dB — hotter than TimGM6mb, the −1 dB limiter absorbs the +12 dB makeup cleanly); sf2 read+load measured 42 ms, so cold-start impact of the bigger file is negligible. Function bundle estimate now ~130 MB incl. ffmpeg — well under the 250 MB limit. `tsc --noEmit` clean. (commit: e8ec32a)
- Instruments is REAL (was a dim "Soon" stub; builds on the `0ef157b` spike): hum/sing a melody → hear it on one of 32 curated GM instruments. Honest copy throughout: we transcribe the notes+timing and REPLAY the tune on a synthesized instrument (monophonic; clear single-note hum works best) — not a timbre transform. Pipeline all-local in `/api/instruments` (synchronous, maxDuration 60): presign-uploaded/mic-recorded vocal (25s / 15MB caps, ownership-checked path) → ffmpeg-decode to 22050Hz mono Float32Array → `@spotify/basic-pitch` on the tfjs **WASM backend** (416ms for 3.2s locally = ~10× the JS-CPU backend; CPU fallback if wasm assets fail; `[timing] stage=basicpitch` logged to retune the cap post-deploy) → min-duration (0.08s) + confidence (amp 0.15) spurious-note filter (0 notes → honest 422 + refund) → `@tonejs/midi` → js-synthesizer (WASM FluidSynth) + bundled `assets/soundfonts/TimGM6mb.sf2` (5.9MB GM soundfont, committed; license note in assets/soundfonts/README.md) → +12dB/limiter → 256k MP3. `INSTRUMENTS_CREDITS=25` + curated instrument map + caps + filter thresholds in `src/lib/instruments.ts`; atomic deduct / refund-on-every-failure / admin bypass (Choir pattern). Result = normal saved track (`kind='instrument'` row, kind-tolerant insert → sign-on-read playback, Saved Tracks, Share, delete; SavedSwapPage labels honestly). DEPENDENCY SURGERY: basic-pitch pins tfjs ^3.2.0 → npm nested a second tfjs (two engines + tsc type-identity errors), and tfjs 3.21's wasm backend can't run the model ("Unknown dtype"); fixed with `overrides` in package.json forcing basic-pitch onto the root tfjs 4.22.0 — verified correct notes + 416ms on the 4.22 wasm backend. Deps now real (`@spotify/basic-pitch`, `@tensorflow/tfjs@4.22`, `@tensorflow/tfjs-backend-wasm@4.22`, `js-synthesizer`); all marked serverComponentsExternalPackages, and `/api/instruments` outputFileTracingIncludes ships ffmpeg + the basic-pitch model + the tfjs .wasm files + the .sf2. Sidebars + dashboard card enabled; middleware gates `/instruments`. `tsc --noEmit` clean. ⚠️ Untested on live; needs migrations `20260712000000/1/3`. (commit: 1a63f5e)
- Instruments feasibility spike (voice → instrument) — VERDICT: **FEASIBLE IN VERCEL**, no separate service needed. Proven locally end-to-end with `scripts/spike-voice-to-instrument.mjs` (standalone; spike deps installed `--no-save`, instructions in header): audio → `@spotify/basic-pitch` in plain Node (accepts raw Float32Array; model ships in-package, 1.9 MB; test melody transcribed exactly and recovered again after rendering) → MIDI → `js-synthesizer` (libfluidsynth WASM = the static-FluidSynth equivalent) + TimGM6mb.sf2 (5.9 MB GM soundfont) → piano WAV. Real footprint ~22 MB (the 295 MB install is dist bloat; loaded runtime measured at 6.8 MB JS), cold start negligible (tfjs import 62 ms). The one constraint: transcription runs ~1.3× realtime on the pure-JS CPU backend locally (slower on a lambda) — v1 must cap input length (~20-30 s) or adopt the tfjs wasm backend. MIDI→audio is free (77 ms). Full findings + build plan in PROJECT_STATUS §6. No feature code, no UI, no app deps touched. (commit: 0ef157b)
- Choir Composer is REAL as a DSP vocal harmonizer MVP (was a dim "Soon" stub; true SATB stays parked — no viable model, and the copy is honest about it: "stacked harmonies of your own voice", never "distinct singers"). Upload OR mic-record a solo vocal (25 MB cap; raw-take mic settings, 2-min cap) → pick 2/4/8 voices × a mode (Thirds & fifths major pad, or Octaves + detune which fits any melody) → `/api/choir` builds the stack synchronously with ffmpeg only (no paid model): pass 1 normalizes any input format to 44.1k WAV; pass 2 is one filter_complex — asplit into N layers, each harmony pitch-shifted timing-preserved via asetrate→aresample→atempo (all preset offsets within ±12st so one atempo suffices; the rubberband filter was avoided since static ffmpeg builds may lack it), lead 1.0 / harmonies 0.55, amix normalize=0 + alimiter −1 dB, 256k MP3. Presets + `CHOIR_CREDITS = 25` live in `src/lib/choir-presets.ts` (shared route/UI). Credits: atomic `deduct_credits()` up front, `add_credits()` refund on EVERY failure path after the charge, admin bypass (Song Studio pattern). Result = a normal saved track: voice-swaps bucket + `kind='choir'` row (kind-tolerant insert; vocalPath must be inside the caller's own storage folder) → sign-on-read playback, Saved Tracks, share, delete; SavedSwapPage labels it "Vocal harmony stack — Choir Composer" with a "Preset" detail (kind labels refactored to a map). Filter graph verified locally with the bundled ffmpeg (6s in → 6s out, duration preserved). Sidebars + dashboard card enabled; middleware gates `/choir`; `/api/choir` added to outputFileTracingIncludes so Vercel ships the ffmpeg binary with it. No migration (reuses `kind` from `20260712000003`), no new grants. `tsc --noEmit` clean. ⚠️ Untested on live; needs migrations `20260712000000/1/3` applied (charge/refund/label). (commit: c545fa5)
- Song Studio is REAL (was a dim "Soon" sidebar stub): AI full-song generation (music + vocals) from lyrics + a style prompt + duration, via ACE-Step (`lucataco/ace-step`, version pinned in new `src/lib/song-engine.ts` mirroring rvc-engine — pinned 2026-07-12 from the live latest_version, full 15-param input schema reviewed; we send tags/lyrics/duration, quality knobs left at defaults). Costs `SONG_STUDIO_CREDITS = 50` (single source of truth), charged ATOMICALLY up front via `deduct_credits()` and refunded via `add_credits()` on failure — refund is idempotent across repeated polls (a result_path-null marker row keyed by the unique replicate_prediction_id index inserts at most once); ADMIN_EMAILS bypass both. Route `/api/song-studio` = stem-split's create+poll shape (POST validates lyrics ≤5000 / style ≤300 / duration 10-240s, deducts, creates; GET polls, and on success copies the output into the voice-swaps bucket and inserts a `kind='song_studio'` voice_swaps row — insert tolerates a deploy that outruns the migration by retrying without `kind`). Generated songs are therefore normal saved tracks: sign-on-read proxy playback (never expires), Recent/Saved Tracks, SHAREABLE via the existing ShareControl, deletable, 90-day retention; SavedSwapPage labels them "AI-generated song — Song Studio" with a "Style" (not "Voice") detail. UI `/song-studio` (auth-gated, middleware matcher extended): title (optional) + style prompt + lyrics textarea ([verse]/[chorus]/[instrumental] hint + placeholder) + 30s-4min duration presets + Generate button showing the 50-cr cost (founder: "free") + elapsed-timer progress + player/download/share/open-in-saved-tracks result card + content disclaimer (AI-generated; no copyrighted lyrics; permitted use only — ACE-Step's license flags copyright responsibility). Sidebar stubs enabled in VSidebar + VLSidebar + dashboard card. Migration `20260712000003_voice_swaps_kind.sql` (adds nullable `voice_swaps.kind`; NO new grants — service_role table-level grants cover it). `tsc --noEmit` clean. ⚠️ Untested on live; needs migrations `20260712000000-3` applied (deduct/refund/share/kind). (commit: c4c71fb)
- Remove the Age Range / Accent / Output Language config toggles entirely (were disabled "Coming soon" stubs since `5dbac6e`) — a feasibility check against the live input schema of the pinned RVC engine (`pseudoram/rvc-v2` version `d18e2e0a…`, all 11 params) confirmed none of the three can be delivered by this pipeline: no age/accent/language parameter exists (age/accent live in the trained model's timbre; accent-bleed control is already the Style Intensity knob; language change would need a transcribe→translate→re-sing pipeline RVC can't do), so "Coming soon" was over-promising. Deleted: the three controls + badges from ConfigStep, their props/destructure, the AgeRange types (both files), ACCENTS/LANGUAGES constants, the ageRange/accent/language state and prop threading in VoiceSwapPage, SegControl's now-unused disabled machinery, and the dead vs-badge-soon/vs-select-*/vs-seg--disabled CSS. The three values were confirmed sent to no API (grep of every fetch body). Working controls (Gender Lock, Style Intensity, Pitch Shift, duet pickers) untouched. PROJECT_STATUS §3 records the not-feasible verdict so this isn't mistaken for a TODO. `tsc --noEmit` clean. (commit: 5c15162)
- Shareable public track links (the Share button is live — was a disabled "Soon · Pro" stub). Opt-in per track: Share mints an unguessable random UUID `share_token` on the voice_swaps row (migration `20260712000002` — column + partial unique index, NO new grants: service_role select/update already cover it) and copies `/s/<token>` to the clipboard; Unshare nulls the token (every distributed link dies instantly; re-sharing mints a NEW token). Public plumbing (no auth, not in the middleware matcher): `GET /api/shared/<token>` returns display metadata only (song/voice/date — no user ids or paths; unknown/revoked/expired all 404 identically), `GET /api/shared/<token>/audio` re-signs result_path fresh on EVERY load and 307s (the sign-on-read proxy pattern — a share link can never hit the stale-signed-URL bug), and `/s/[token]` is a minimal branded listener page (play + "Made with MausamVox" CTA). Owner UI: shared `ShareControl` component wired in ResultStep (stub replaced; VoiceSwapPage now keeps the persisted swapId — Share disabled with a "saving…" hint until the save lands, reset on new-swap/regen) and in SavedSwapPage (share state loads from the row; revoke available any time later). Share requires stored audio (409 on expired rows); deleted rows 404 via the token lookup. `tsc --noEmit` clean. ⚠️ Migration NOT applied — run `20260712000002` in the founder SQL Editor; until then Share errors with a toast (everything else unaffected). (commit: 9cf7704)
- Split ALL audio formats into training clips in prepare-dataset (was WAV-only) — mp3/m4a/webm uploads were packaged as ONE giant clip (route.ts's `splitAudio` fallback), starving RVC training of samples, a likely cause of weak clones. Non-WAV input is now decoded to 16-bit PCM WAV (mono, 44.1 kHz, `-bitexact`) via the new `ffmpeg-static` dependency (binary ships with the deploy; `serverComponentsExternalPackages` + `outputFileTracingIncludes` in next.config.mjs make Vercel trace it), then run through the SAME `splitWav` used for native WAV (10s clips, ≥2s minimum). WAV path byte-for-byte untouched. Graceful degradation: any conversion failure (bad file, missing binary) logs loudly and falls back to the old single-clip packaging — dataset prep can never break, worst case is the previous behavior. Verified locally end-to-end: 25s mp3 → 3 clips (10s/10s/5s) through the exact route logic; ffmpeg's WAV header parses at the fixed fmt offsets splitWav reads. ⚠️ Needs a live test (mp3 express-clone upload → Vercel log should show "non-WAV audio converted" + clips > 1). (commit: c250c7e)
- Finish making ALL credit mutations atomic — the last two read-then-update spots from the `4a7ea18` sweep. (1) The lyrics post-success 25-cr charge (`/api/lyrics`) now calls `deduct_credits()`, keeping its best-effort contract: INSUFFICIENT_CREDITS → warn + skip the charge (lyrics kept), any other RPC failure → logged, never fails the already-successful transcription. (2) gender-split's `refundCredits` now calls new `public.add_credits(p_user_id uuid, p_amount integer)` (migration `20260712000001_add_credits_fn.sql`): a single relative-increment UPDATE returning the new balance, raises USER_NOT_FOUND/INVALID_AMOUNT, EXECUTE revoked from public/anon/authenticated and granted to service_role only, named generically for future top-ups. Sweep re-run confirms no inline `credits_remaining` writes remain in `src/`; preview charge/refund were already atomic in SQL (`consume_preview` = SELECT…FOR UPDATE row lock, `refund_preview` = relative increment). `tsc --noEmit` clean. ⚠️ Migration NOT applied — run `20260712000001` (with `20260712000000`) in the founder SQL Editor; until then refunds are logged no-ops and the lyrics charge silently skips. (commit: cd0d8d9)
- Use the atomic `deduct_credits()` RPC in the gender-split route — replaces the inline read-then-update debit (the same race closed in `5d638d4` for the deduct route); INSUFFICIENT_CREDITS maps to 402 identically. The now-redundant pre-check balance gate was removed (the RPC checks atomically); plan gate, admin bypass, refund-on-create-failure, and `chargedUserId` flow unchanged; profile fetch narrowed to `plan` only. ⚠️ Same dependency: migration `20260712000000` must be applied or the debit 500s. Sweep result (asked to confirm this was the last one): the lyrics route's post-success charge (`/api/lyrics`, ~line 386) is still a read-then-update deduction — left as-is, it's deliberately best-effort/non-blocking; gender-split's `refundCredits` is a read-then-update *increment* (racy in theory, not a deduction); voice-convert already uses the atomic `consume_preview` RPC. `tsc --noEmit` clean. (commit: 4a7ea18)
- Make credit deduction atomic — closes the read-then-update race in POST `/api/credits/deduct` where two concurrent requests could both pass the balance check against the same stale value and over/under-spend. New migration `20260712000000_deduct_credits_fn.sql` creates `public.deduct_credits(p_user_id uuid, p_amount integer)`: a single conditional UPDATE (`where id = p_user_id and credits_remaining >= p_amount`) checks-and-decrements atomically and returns the new balance; raises `INSUFFICIENT_CREDITS` (row untouched) / `USER_NOT_FOUND` / `INVALID_AMOUNT` otherwise; EXECUTE revoked from public/anon/authenticated, granted to service_role only (SECURITY INVOKER — service_role already holds update on users). Route now calls the RPC instead of select-then-update and maps the errors to 402/404/400; amount validation tightened to whole numbers; response shape unchanged. `tsc --noEmit` clean. ⚠️ Migration NOT applied — must be run in the founder SQL Editor before the deployed route works (until then deductions 500). Gender-split's inline deduct still read-then-update (out of scope, noted in PROJECT_STATUS §3). (commit: 5d638d4)
- Add "Operating Rules" (standing instructions for the working model) to `CLAUDE.md` — 10 trigger→action procedures (intent reading, problem breakdown, verification, known-vs-guessed labels, self-attack, completeness, delivery, fake-competence scan) plus a final pre-send gate. Loaded automatically every session. Existing sections (How I work / gotchas / handoff protocol) untouched. (commit: eac2b4e)
- Sync `.env.local.example` with the vars the app actually reads (verified by grepping every `process.env.*` in `src/`): the two public Supabase keys + `SUPABASE_SERVICE_ROLE_KEY`, `REPLICATE_API_TOKEN`, `MVSEP_API_TOKEN`, and optional `RVC_ENGINE` (bare default / cover rollback, commented). No RUNPOD vars. (commit: edf1220)
- Landing honesty: remove unimplemented BPM/key-detection claims — Pricing Starter feature now reads "4-stem split" (was "+ BPM/key"), and the Smart Stem Studio card drops "BPM and key detection. In-browser per-stem editing." (surrounding true copy kept). Repo-wide grep confirms no other occurrences; onboarding's sample-track "140 BPM" labels are demo metadata, not claims. (commit: f027a87)
- Remove the dead RunPod module: deleted `src/lib/runpod.ts` (imported nowhere), corrected PROJECT_STATUS/README tech-stack + env-var docs — training runs on Replicate (`replicate/train-rvc-model`), not RunPod/GPT-SoVITS. `.env` files had no RUNPOD vars. `tsc --noEmit` clean. (commit: e61d07b)

## 2026-07-07

- Pre-warm the karaoke (UVR KARA_2) pool at swap start (speed step 2). Mirrors the existing RVC `fireWarmPing`: when Demucs (stem-split) kicks off, also fire a tiny throwaway karaoke-split prediction so the real karaoke-split — which runs the instant the stems land, ~120–155s later — skips the ~19s cold-start queue. New `src/lib/karaoke-engine.ts` holds `KARAOKE_VERSION`/`KARAOKE_MODEL` (now the single source of truth, imported by the karaoke-split route) + `fireKaraokeWarmPing`. Fired ONCE at stem-split POST (a GET-time re-ping would race the real job, unlike the RVC re-ping). Best-effort/non-blocking (only the create is awaited, all failures swallowed) — never delays or breaks the pipeline, no quality/behaviour change. ~$0.002/swap. Test: a cold swap's karaoke-split `[timing]` line should show a much lower `queue_ms`. (commit: 8cde734)
- Extend the Bass/Treble Polish range — Bass to ±16 dB (from ±12), Treble to ±20 dB (from ±12). Range only: defaults unchanged (Studio bass/treble stay 0, so default swaps are unaffected). Split the single `EQ_MAX_DB=12` into named tunable `BASS_MAX_DB=16` / `TREBLE_MAX_DB=20`, used for both the mixStems clamp and the PolishKnob min/max. Step stays 1 dB, centre detent + double-click-to-0 unchanged, dB readout accurate at the new extremes. Byte-identical-at-0 untouched (node-insertion guards unchanged), and the post-EQ −1 dBFS limiter still catches big boosts. (commit: 6047666)
- Fix the polish-persistence bug (Option 2 — auto re-persist on settle): the saved track (Recent Swaps / Saved Tracks) now always reflects the CURRENT polish, not just the initial Studio-default the old one-shot persist captured ~1s after the mix first became ready. ResultStep drops the happy-path one-shot latch; a settled-polish signature drives re-saves — each time it changes from what was last stored, the current mix re-uploads (after the existing ~1s settle timer) and UPDATEs the row. Deduped by signature (no redundant uploads), in-flight-guarded (a tweak during upload is re-checked on completion), best-effort (a failed re-save keeps the previous version, no hammering). Music-only backing is polish-independent → saved once on the first save. `/api/voice-swaps/persist` gained a RE-SAVE branch: an existing row (same prediction id) re-uploads to fresh VERSIONED paths (proxy never serves a stale cached object), UPDATEs result_path/instrumental_path, then removes the superseded object after the DB points away — no Replicate fetch, no credits. VoiceSwapPage keeps the persist context armed + adds a silent re-save path. A subtle "Saved ✓" pip flashes on each (re)persist. ⚠️ Requires migration `20260707000000` (grant UPDATE on voice_swaps to service_role) — MUST be applied via founder SQL Editor before re-saves take effect; until then the UPDATE 403s and the re-save is a silent no-op (first save / playback / download unaffected). (commit: 575ff3d)
- Add a default "Studio" polish preset so new swaps come out finished, not dry (ResultStep.tsx only). New swaps now start the polish controls at tasteful defaults via named tunable constants: STUDIO_WARMTH_DB=4 (+4 dB → knob 40), STUDIO_REVERB_WET=0.15 (15% wet → knob 30), STUDIO_ECHO_WET=0, STUDIO_BASS_DB=0, STUDIO_TREBLE_DB=0 — applied to both tabs and the saved/downloaded file. Byte-identical-at-0 machinery unchanged (node-insertion guards untouched; 0 = true bypass) — only initial values changed. Panel: "Reset to defaults" now applies the Studio preset and a new one-tap "Raw" zeros all five controls for an instant finished-vs-dry A/B; honest foot note added. New swaps start at Studio (ResultStep remounts, step===3 no key); regenerate preserves a user's customized values (stays mounted, no forcing effect). VOCAL_MAKEUP (mix balance) untouched; Raw zeros only the five polish controls. (commit: d941ead)
- Add a vocal makeup gain to lift the clone against the backing in the final mix. The separated-then-reconverted vocal sits low (no mix-bus makeup on the isolated stem), so `mixStems` now applies a named `VOCAL_MAKEUP = 1.3` (+2.3 dB) to the converted-vocal channel only: `vocalGain = VOCAL_MAKEUP / √N`. Music stays at 0.8; `rms_mix_rate` untouched (isolated to one lever for a clean regression listen). Independent of the Warmth/Bass/Treble EQ — it rides the always-present source gainNode, never the conditional EQ-node insertion, so the byte-identical-at-0 Polish guarantee is intact. Applies everywhere mixStems runs (full-song both A/B sides + the saved/downloaded file); the −1 dBFS headroom limiter still catches new peaks. Global default — needs a regression listen on a normal loud-vocal track. VOCAL_MAKEUP is the single tuning knob (1.2 gentler, 1.0 = original). (commit: 470e4dc)
- Add unified per-stage `[timing]` instrumentation to the voice-swap pipeline (speed work step 1 of 2 — logging only, no pipeline/param/order changes). New greppable format `[timing] stage=<x> ms=<n> [cold= queue_ms= compute_ms=]` via two helpers in `replicate-timing.ts` (legacy `TIMING` lines kept). Vercel (server) stages: `demucs` (stem-split), `karaoke` (karaoke-split), `mvsep` (gender-split, duet only, wall-clock, cold=n/a), `rvc` (voice-convert) — each with cold/queue/compute from Replicate metrics where available; `cold` reflects container/pool cold-start (the bare RVC cog re-downloads the model every run, so that always sits inside compute). Browser-console (client) stages: `mix` (in-browser mix/master), `upload` (encode + persist), `total` (client-observed convert phase per full swap). (commit: 8266c2c)
- Add Bass and Treble EQ knobs to the Polish panel — two bipolar shelving EQs alongside Warmth (kept as-is), matching the Warmth/Reverb/Echo pattern exactly: client-side Web Audio, free, applied to both the Full-song and Vocals-only tabs and baked into the saved/downloaded WAV/MP3. Bass = low-shelf (~100 Hz), Treble = high-shelf (~8 kHz), each −12..+12 dB, default 0. Byte-identical at 0 dB by construction (no BiquadFilter node inserted at 0, same guarantee as Warmth). Chained as tone EQ (Warmth → Bass → Treble) before the Reverb/Echo time effects; music bed never coloured. Reuses the PolishKnob dial, generalized with optional min/max/step/resetTo props — defaults keep Warmth/Reverb/Echo byte-identical, bipolar ranges fill the value arc from a centre detent and double-click resets to 0. Full debounce/settle/persist wiring mirrors warmth. Panel order: Warmth, Bass, Treble, Reverb, Echo (row wraps for five knobs). (commit: 8fb9587)

## 2026-07-06

- Add a conservative hum-hallucination filter to the lyrics generation route (Scenario A from the §6 investigation; no engine switch). `dropHallucinatedWords(words)` runs at the word level before `groupWordsIntoLines` (and after per-word romanization for the romanized path), stripping only the clear Whisper machine signature: a maximal run of the same normalized token (case- + Latin-diacritic-insensitive, punctuation-stripped) or a repeated A-B bigram, that is both long AND crammed denser than singable. Thresholds (report for tuning): unigram ≥6 identical, bigram ≥4 cycles (≥8 tokens), AND ≥6 tokens/sec density (span=0 → infinite). Removed words fall through to the existing gap/♪ + countdown path (no empty line objects; if all filtered, the honest no-lyrics path fires — nothing stored/charged). Verified real hooks survive (la×3, alaap o×5, na hook ×6 ~3/s, o-na bigram slow) while dense/degenerate garbage is dropped. Also logs the prediction output's top-level + first-chunk keys each run (passive probe for confidence fields; nothing branches on it). Caveat copy updated. Existing rows need a 25-cr regenerate to benefit; no migration/grant/column. (commit: 4be0b03)
- Add a minimise/expand toggle to the "Sing over it" (KaraokePanel) panel — on pages showing both Perform Live and Sing over it, the karaoke panel stayed fully expanded and crowded the page. A chevron control (▾/▸) in the header collapses the panel to just its header bar (freeing the vertical space) and expands it again; default expanded. UI only: the body is `display:none`'d when collapsed (not unmounted) so the hidden backing `<audio>`, LyricsPane state, and recording refs survive, and the control is disabled while recording so an in-progress take (and its Stop button) can't be folded away — recording safe by both the disable and the keep-mounted approach. Recording/lyrics/clock/word-highlight/scroll/generation untouched. (commit: 576a763)
- Fix per-word lyric spans running together ("mainkararahahoon"). The space was a leading character inside each `inline-block` word span, and CSS trims leading/trailing whitespace inside an inline-block, so it disappeared. Fix: emit the space as a sibling text node between spans (keyed Fragment) — whitespace between inline boxes renders as a normal wrappable space, and being outside the spans it never scales/highlights with the active word. Active-word highlight/scale/glow, wrapping, the rAF span query, and the whole-line fallback all unchanged. (commit: f71190b)
- Fix lyrics auto-scroll + per-word highlight not working in "Sing over it" (KaraokePanel) during backing playback. Root cause: the idle "Backing track" preview plays through AudioPlayer's own internal `<audio>` element, not the hidden element wired to LyricsPane — so pressing play advanced neither `backTime` (its `onTimeUpdate` is on the hidden element) nor the `playing` gate (`recState==='recording'`, false while idle), freezing `currentIdx` (no scroll) and never starting the per-word rAF loop (no highlight). The prior scroll fix `6d43696` was correct but never ran here because the clock genuinely didn't move. Fix: AudioPlayer gains optional `onTimeUpdate`/`onPlayingChange`/`mediaRef` props (additive; other callers unaffected); KaraokePanel wires the idle preview player to drive `backTime` + a new `previewPlaying` flag and points a `lyricsClockRef` at whichever backing element actually plays (preview element while previewing, hidden element while recording), passing `time`/`playing`/`audioRef` to LyricsPane accordingly. Now follows + word-highlights in both preview and recording, matching Perform Live; LyricsPane itself unchanged. (commit: cc69c6d)
- Add show-more/show-less paging to the `/swaps` Saved Tracks list (reached via the dashboard's Recent Swaps "View all"). It previously rendered every saved swap at once, forcing a long scroll. Front-end display only — the fetch, query, ordering, row styling, and Open action are unchanged; it just caps how many already-loaded rows render: 5 most recent by default, "Show more" reveals 5 more per click, "Show less" collapses back to 5 once expanded, and neither button shows with ≤5 swaps. (commit: 1bba354)
- Fix lyrics-pane auto-scroll in "Sing over it" (KaraokePanel) — the current line scrolled off and never re-centred there, though it worked in Performance Mode. Root cause: the auto-scroll effect used `el.scrollIntoView({block:'center'})`, which scrolls every scrollable ancestor up to the document; Performance Mode locks the page (fixed overlay + body overflow hidden) so only the pane moved, but the KaraokePanel mount is in normal page flow, so the browser centred the line by scrolling the PAGE and left the pane visually still. Fix: scroll the pane container directly via a computed target `scrollTop` (line centre in the pane's scroll coords − half the pane height) + `pane.scrollTo`, confining every scroll to the pane so both mounts follow the clock identically. Manual-scroll pause + programmatic-scroll guard unchanged (tap-to-seek still resyncs; a user drag still pauses auto-follow ~3s). Word highlighting, spent-line styling, and the countdown untouched. (commit: 6d43696)
- Word-by-word lyric sync — STEP 2 (the renderer); generation route untouched. Within the CURRENT line only, `LyricsPane` now renders per-word spans (a new memoized `WordLine`) and lights each word as it's sung: dim = not-yet, white = already-sung, bright pink + scale + glow = the active word (focal point). The word clock is a `requestAnimationFrame` loop reading the host `<audio>` element's `currentTime` (NOT the ~4/s `timeupdate` event); it toggles classes imperatively on spans queried from the current line's DOM node (robust to seek direction), only touches the DOM when the active word changes, and never setStates per frame. `displayItems` is memoized so the `words` arrays keep a stable identity across time-ticks — that's what lets `WordLine`'s `memo` skip re-renders and preserve the imperatively-set classes rather than React resetting className 4×/second. Fallback is detected per-LINE: a line renders word spans only when it has a `words` array AND the host wired an audio element; old rows (no words) and edited lines (the edit save now also strips words locally to match the PATCH route) keep whole-line highlighting. rAF is cancelled on pause / line change / lyrics change / unmount — no leaked frames. Both hosts pass `audioRef` + `playing` (PerformanceMode: its audio + playing state; KaraokePanel: the backing element + `recState==='recording'`). Gaps and the item-3 come-in countdown are untouched. `next build` clean. (commit: 29f05b0)

## 2026-07-05

- Word-by-word lyric sync — STEP 1 (generation/storage) only; renderer deferred pending founder go. `/api/lyrics` now transcribes with `timestamp: 'word'` and regroups the per-word chunks into the same phrase-style display lines via a new `groupWordsIntoLines` heuristic (break on a >0.8s silence gap, a 9-word cap, an 8s cap, or sentence-final punctuation), nesting each line's `words: [{text,start,end}]` array inside the existing `lines` jsonb. Romanization moved to per-word (each word transliterated before grouping) so romanize.ts's whole-word corrections still apply and each line's text stays the join of its words. Confirmed no migration / no new column / no new grant — pure jsonb nesting, existing INSERT/UPDATE grants cover it. Old rows (no `words`) and today's renderer are byte-for-byte unaffected (line-level start/end/text unchanged); an edit still stores only start/end/text, dropping the row's words as designed. The word-highlighting renderer is NOT built — waiting on founder confirmation of the stored shape first. (commit: 0ed8eb7)
- Investigate word-by-word lyric sync (`timestamp: 'word'`) — findings only, NOT built, awaiting founder go/no-go (recorded in PROJECT_STATUS §6). Key results: the cog returns the same `{chunks:[{text,timestamp}]}` shape with one WORD per chunk, so a single word-level call is a superset (group words→lines ourselves = one prediction, one charge, serves both display and highlighting); existing 'chunk' rows would need a 25-cr regenerate to gain word data; storage = an optional `words` array nested in each line object in the existing jsonb (no migration/column/grant, old rows render as today); renderer highlights only the current line's word spans driven by a requestAnimationFrame clock (the `<audio>` timeupdate event is too coarse at ~4/s); Hindi-romanized must transliterate per word; effort Medium (~one session). (investigation only — no code commit)
- Lyrics v1.5, items 1-3 — wire the shared lyrics pane into "Sing over it" + spent-line styling + come-in countdown. **Item 1:** extracted the whole lyrics experience (load-if-stored, paid generate + regenerate, synced auto-scroll, ♪ gaps, edit modal) out of PerformanceMode into a shared `src/components/karaoke/LyricsPane.tsx`, and mounted it in KaraokePanel — the recording panel where lyrics matter most. PerformanceMode now just feeds the pane the stem path, playback clock and a seek handler; both KaraokePanel call sites (saved-swap page, Stem Studio) pass the vocal-stem source key. In the recording panel lines are deliberately non-tappable (seeking the backing mid-take would misalign the recording) and follow the hidden backing element's clock. The "v1 — no lyrics display yet" badge and "lyrics display coming later" copy are removed. **Item 2:** finished lines now read as clearly "spent" (color #2B2B45 / opacity 0.4 — much dimmer than the upcoming #7878A0) and the current line is a bright brand gradient text fill (was solid white). **Item 3:** instrumental gaps over ~5s (was 8s) show the ♪ marker whose final 3 seconds become a pulsing 3-2-1 come-in countdown so the singer knows when to enter. `next build` clean. (commit: cb7d2ea)
- Lyrics v1.5, item 5 — Hinglish spelling corrections for common Hindi words. A curated whole-word correction pass in `romanizeDevanagari` (`src/lib/romanize.ts`) fixes the two most visible artefacts for the commonest lyric words: Sanskrit inherent vowels (kara→kar, dila→dil, hun→hoon) and dropped nukta sounds (jindagi→zindagi, philma→film, jara→zara). Applied per alphabetic token so punctuation and mixed-in English pass through untouched; curated for low collision with a few accepted homograph trade-offs (pyara→pyaar). It's a dictionary, not a general schwa/nukta algorithm — words outside it keep the known quirks, and the honest caveat + per-line edit modal remain the real fix. (commit: 532c06c)
- Add the "Hindi (romanized)" lyrics option — the answer to "I want Hindi lyrics in Latin letters, not Devanagari". Language dropdown is now Auto-detect / **Hindi (romanized)** (the default Hindi choice) / Hindi (Devanagari) / English. Romanized transcribes with `language=hindi` and then transliterates each line server-side in `/api/lyrics` before storing: new `src/lib/romanize.ts` on `@indic-transliteration/sanscript` (MIT, maintained Sanscript port) devanagari→IAST, scholarly diacritics stripped for casual readability, plus targeted fixes (danda stripped; anusvara/candrabindu → "n" so मैं→"main"/हूँ→"hun"; nukta consonants fall back to base since Sanscript's Sanskrit-era scheme lacks them). The stored lines ARE the romanized text (cache/edit/display need no script awareness) and `track_lyrics.language` stores the hint value itself (`hindi-rom`/`hindi-deva`) — each row records its language+script variant with no new column/migration. Known accepted quirks logged in PROJECT_STATUS (extra inherent vowels "kar"→"kara"; "zindagi"→"jindagi"): the edit modal is the remedy. (commit: d63cf4f)
- Add the lyrics Regenerate flow + honest language guidance — the founder's Hindi track transcribed with hint=English came out part-translation/part-Hinglish and the permanent cache offered no redo. "↻ Regenerate" now sits next to Edit in the pane foot: a confirm dialog warns the current lyrics and any edits will be lost and that it charges 25 cr again, reopens the language choice, then force-retranscribes — POST `force` skips the cache short-circuit and the success poll REPLACES the stored row in place (upsert on the `(user_id, source_key)` unique key; covered by the table's existing insert+update grants, so the deferred "re-transcribe needs an UPDATE-or-DELETE grant decision" resolved to: no new migration). A failed regenerate keeps the stored row, falls back to the existing lyrics with a visible notice, and charges nothing. Language honesty in the same commit: investigated the cog's task param — `task: 'transcribe'` was already pinned (its `translate` task is never used), but the hint is Whisper's DECODE language, so English-on-Hindi translates regardless; new copy under both dropdowns says to pick the language the song is SUNG in, and the pane foot now labels which language the stored lyrics were generated with. (commit: 17452f0)
- 2026-07-05 first acceptance pass result (founder): warm-ping fixes PASS — fresh swap `[voice-convert] TIMING` queue 34.6s / compute 33.1s / total 67.8s, the speed milestone is closed; music-only Perform Live, headroom limiter, and favicon all PASS. Lyrics v1 mechanics ran but the language handling failed the real-world test (the mistranscription above) — fixed same day. (test session — no code commit)
- Session wrap-up — both 2026-07-05 migrations confirmed applied (founder SQL Editor runs; migrate.mjs keeps no ledger, idempotent files make manual runs equivalent), lyrics Stage 1 confirmed live through `997a002`, and PROJECT_STATUS's START HERE consolidated to the full untested list for one founder pass next session: warm-ping TIMING (queue ≈ 0 target), music-only Perform Live, headroom limiter, favicon, and the complete lyrics flow (generate / sync / edit / caching / credits / honest absence on legacy rows). (docs only — no code commit)
- Add the lyrics edit modal — "✎ Edit lyrics" lists every line with its timestamp for in-place text fixes and 🗑 deletion of junk lines (humming heard as words, misheard ad-libs); emptied lines count as deletions, one line must remain, timestamps deliberately not hand-editable (no waveform = guesswork). PATCH saves with the app-code ownership check and sets the `edited` flag; Escape closes the modal not the overlay (capture-phase listener). (commit: fc3b49a)
- Build the synced auto-scrolling lyrics pane in Performance Mode (v1, single-color). New `lyricsSourceKey` prop (the durable vocal-stem path): stored lyrics load on open; otherwise the overlay offers "Generate lyrics · 25 cr" with a language-hint dropdown (Auto/Hindi/English) and honest copy (AI transcription of singing WILL have mistakes; mixed-language and humming caveats; everything editable). Pane: dimmed done lines / bright glowing current line / muted upcoming, smooth scrollIntoView centering, manual-scroll pause with ~3s resume (programmatic scrolls don't count), tap-to-seek, ♪ markers for >8s instrumental gaps, fade mask, 300ms highlight lead. Timestamps are from the ORIGINAL vocal stem — RVC preserves timing, so they fit the swapped track and music-only backing alike. Wired on /swaps/[id] (both Perform Live buttons) and Stem Studio (captures the poll's vocalsPath); no key (legacy rows, manual stems) = no lyrics UI at all. (commit: 73c75ae)
- Persist `vocal_stem_path` at swap save — the client's `stemResult.vocalsPath` rides the persist body into the new column, giving /swaps/[id] its pointer back to the stems (the lyrics key). The deploy-before-migrate retry is generalized: the insert strips whichever optional column (instrumental_path / vocal_stem_path) the PGRST204 error names and retries, so a swap is never lost to schema lag. (commit: 7c447d7)
- Ship `/api/lyrics` (POST/GET/PATCH) — POST signs the durable vocal stem and starts `vaibhavs10/incredibly-fast-whisper` (Whisper large-v3, `timestamp: 'chunk'`; native word timestamps keep a word-karaoke upgrade open for all languages, unlike WhisperX's per-language aligners), short-circuiting to the stored row when one exists (reopen = instant + free). GET polls: on success parses chunks (null-end guard, empty filter, sorted), stores the `track_lyrics` row and charges 25 cr server-side on success only — ADMIN_EMAILS bypass, the unique-index race loser never double-charges, failures and zero-line results (instrumental/too-quiet, honest error) charge nothing and store nothing. PATCH saves edits with the ownership gate. `[lyrics] TIMING` logged like the other stages. (commit: ef3e5ac)
- Create the lyrics data model — `track_lyrics` (timed lines jsonb, keyed `(user_id, source_key)` where source_key = the durable vocal-stem path, so ONE transcription serves every swap/fine-tune/regenerate born from a split AND Stem Studio tracks with no swap row; content-hash keying for re-uploads considered and deferred) with its service_role SELECT/INSERT/UPDATE grants in the same file (voice_swaps pattern: no RLS, admin client + app-code ownership; DELETE deliberately not granted until a cleanup flow exists) + `voice_swaps.vocal_stem_path` (no new grant — table-level grants cover new columns). Idempotent; ⚠️ needs a founder SQL Editor run. (commit: 6d24617)
- Add a proper favicon set from the header logo mark — the app had none at all. Next App Router file conventions, exact LogoMark geometry/gradient (waveform bars, purple→pink→cyan): `src/app/icon.svg` (modern browsers), `icon.png` 64px (Safari/non-SVG), `apple-icon.png` 180px on the #05050F page background with an inset mark (iOS fills transparency with black; covers save-to-home-screen), and a 32px legacy `favicon.ico`. Next injects the link tags automatically. (commit: 4187236)
- Store a music-only instrumental per swap and offer "Perform live · music only" on `/swaps/[id]` — the deferred per-swap-instrumental item, smallest version. ResultStep's deferred one-shot persist now renders a sibling music-only mix (same bass/drums/other bed at the same 0.8 gain, no vocals, no polish; mixStems accepts an empty vocal list for it) and uploads it alongside the full mix, strictly best-effort. Migration `20260705000000` adds `voice_swaps.instrumental_path` — explicitly NO new grant needed (all existing grants are table-level and cover new columns; the recurring gotcha is per-operation and this adds none). Persist stores it as `<user>/<swapId>-instrumental.mp3` with a deploy-before-migrate safety (insert retries once without the column); the swap proxy serves it as `instrumental.mp3`; delete removes both files; the saved-swap page shows the second Perform Live button when the backing exists and an honest fine-print fallback on older rows. ⚠️ Migration committed but NOT applied — founder `migrate.mjs` run required. (commit: ea78cbd)
- Add a −1 dB headroom scale to mixStems before encode — the Warmth low-shelf (up to +10 dB) could push a hot mix past full scale, and encodeWav/encodeMp3 hard-clamp into audible clipping with no limiter anywhere. Post-render peak scan + one transparent gain scale, applied only when the peak exceeds −1 dBFS; covers the preview player and the saved file. Found during the 2:37-artifact investigation (which itself turned out to be raw RVC output, not clipping — recorded as a known bare-engine limitation in PROJECT_STATUS §6 with the protect-nudge workaround; the karaoke-split deep-clean pre-pass is deferred unless the artifact recurs across songs). (commit: 923e9e3)
- Fix the underdelivering warm ping with a double ping + `/api/rvc-warm` — the acceptance swap's voice-convert still queued 146.5s cold. Replicate-timeline forensics: the ping fired instantly at swap start, but the shared bare-RVC pool re-chills in UNDER 7 minutes (vs the 18-min probe estimate), so Demucs (~3-4 min) + think-time outran it; the quoted TIMING was a second conversion 30 min later with no ping at all. Fixes: shared `fireWarmPing()` helper (engine-check + swallow-all inside); stem-split pings at POST and again in the GET success handler (idle clock restarts when the stems land); new auth-gated, rate-limited `POST /api/rvc-warm` fired on Result-screen mount so regenerates and fine-tune applies minutes later also land warm. ~$0.002–0.003/swap. Honest residual recorded: idling >5–7 min on the configure screen can still hit a cold pool — unfixable on a shared pool without the rejected $591/mo deployment. (commit: 3629538)
- 2026-07-05 acceptance pass result (founder): bare-RVC engine quality PASSES (compute 33.6s, identity good, distinct regenerate ids), Saved Tracks/sidebar wiring PASSES, Performance Mode PASSES incl. real-phone checks. Two follow-ups spun out of the pass — the cold-queue TIMING (fixed `3629538`) and a 2:37 audio artifact (investigated: in the raw RVC output, not the mix; recorded as a known limitation). (test session — no code commit)
- Session wrap-up — added a "START HERE NEXT SESSION" block to PROJECT_STATUS consolidating the pending acceptance tests for the day's three untested deploys (bare-RVC engine `56eaf29` incl. TIMING/regenerate/preview checks and the `RVC_ENGINE=cover` env rollback; Saved Tracks/sidebar wiring `4348b83`+`971b8b1`+`417ef59`; Performance Mode `c1391f9`+`74db470` incl. the real-phone wake-lock/lock-screen/interruption checks). Founder tests everything in one pass next session. (docs only — no code commit)
- Add the second Performance Mode entry point on the saved-swap page — "🔊 Perform live" next to "Sing along", streaming the existing `result.mp3` signing-proxy URL directly into the overlay (srcUrl path, zero decode) and labeled honestly as the full track including the recorded vocal (a duet with your clone). (commit: 74db470)
- Build Performance Mode v1 (`PerformanceMode.tsx`) — the approved no-recording live player for karaoke nights/buskers/practice: full-screen overlay with an oversized play/pause, restart, coarse seek bar, elapsed/remaining readouts, ✕/Escape exit, and the honest line "Plays out loud — nothing is recorded" (the file contains no `getUserMedia` call by design — the whole point vs KaraokePanel's headphones requirement). Mobile care per the investigation: screen wake lock held while playing (structural typing + feature detect, silently absent where unsupported, re-acquired on `visibilitychange`), Media Session metadata + play/pause handlers for lock-screen controls, and two backing paths — stems decoded + summed via KaraokePanel's `sumBuffers` (now exported) into a WAV object URL, or a `srcUrl` streamed untouched into the `<audio>` element to keep big files off phone memory. First entry point: Stem Studio results, "🔊 Perform live" over the bass/drums/other instrumental. Deferred as recorded: /swaps-row shortcut, persisted per-swap instrumentals, playlists, lyrics. (commit: c1391f9)
- Add a "View all →" link on the dashboard's Recent Swaps header pointing at the new `/swaps` Saved Tracks index — Recent Swaps caps at the latest 4, and this is now the path to the rest. (commit: 417ef59)
- Build the `/swaps` Saved Tracks index — the dashboard's Recent Swaps shows only the 4 newest playable rows, so older saved tracks were unreachable anywhere in the app. New `SwapsIndexPage` lists ALL playable `voice_swaps` (the dashboard's exact authenticated browser-side SELECT minus `limit(4)` — the `grant select to authenticated` from `20260619000000` already covers it, no new grants), newest first, each row opening the existing `/swaps/[swapId]` play/download/delete page; honest empty state pointing at `/voice-swap`; the existing `/swaps/:path*` middleware matcher auth-gates the index with zero matcher changes. Sidebar item renamed "Projects" → **"Saved Tracks"** (finished outputs, not editable projects — stems/settings aren't stored; matches the dashboard's stat name), un-dimmed and linked in both sidebars. (commit: 971b8b1)
- Wire the sidebar "My Voices" item to a real destination (zero-build option, as approved): it now links to `/voice-lab`, whose right panel is already the complete voices list with open/delete and status — no duplicate page built. Live clone-count badge kept; LIBRARY entries gained an `href` field where `null` keeps the dim+Soon treatment (Marketplace and the three unbuilt tools unchanged). (commit: 4348b83)
- SWITCH the RVC engine to the bare pseudoram/rvc-v2 cog + ship the pre-warm ping (the speed work's payoff commit). voice-convert's prediction create now defaults to the bare cog — conversion-only, ~20–40s compute vs the old AICoverGen pipeline's ~140–220s spent re-separating our already-isolated vocal — with the quality already founder-approved from the offline A/B. Input mapping as planned: `input_audio`, plain-semitone `pitch_change`, `f0_method: 'rmvpe'`, the four Fine-tune params 1:1 (panel unchanged), WAV output kept, no seed (per-request re-signed vocal URLs make Replicate dedup impossible), no reverb stage (Polish layer owns reverb). Model still fetched via the signing proxy — the bare cog parses filenames with urlparse+query-strip and re-downloads every run, so neither the Errno-36 nor the wrong-weights cache hazard applies on that path; comment updated to cover both engines. New `src/lib/rvc-engine.ts`: `RVC_ENGINE` env flag, absent/unset = `bare` (no Vercel dashboard step to adopt), `cover` = byte-identical old path for env-flip rollback. stem-split now fires a pre-warm ping after starting Demucs when the engine is bare: one tiny built-in-voice prediction on new `public/warm-ping.wav` (1s sine, ~$0.001) — probe data showed the pool cold-boots in up to ~5 min but warms instantly on a wake, and the ~3-min split window is exactly that runway; create-only await, failures logged and swallowed. Poll/TIMING/credits/refund logic untouched; `next build` clean. NOT yet validated on live — acceptance test listed in PROJECT_STATUS §6. (commit: 56eaf29)
- Close the bare-RVC cold-start question (speed work; investigation only, no route changes). Founder verdict first: original arm C (production-matched params) passes the ear test — identity intact, pronunciation fine on re-listen; C2/C3 variants unnecessary; cog switch approved in principle with cold-start as the sole blocker. Probes via new standalone `scripts/ab-rvc-cold-probe.mjs` (tiny built-in-voice predictions, seconds of compute each): P1 cold 296s queue ~1h after last use; P2 immediately after = 0.0s queue (wake → warm confirmed); P3 after 18 min idle = 121s (warmth gone); P4 lost to a local network error mid-run — not re-run, P1–P3 answer the design question (probe clip cleaned from storage manually). Economics: both cogs run T4 ($0.000225/s); min-1 deployment ≈ $591/mo vs ≈$4.85/mo total current RVC spend at ~5 fresh swaps/day → deployment rejected; adopted strategy = event-driven pre-warm ping fired at swap start (the ~3-min Demucs+karaoke window covers the cold boot; ~$0.001/swap). Also read the pseudoram cog source ahead of the switch: filename derived via urlparse with query stripped (the 255-char Errno-36 bug doesn't exist there; keep using the proxy URL), `overwrite=True` re-downloads the model every prediction (no wrong-weights cache class; measured computes already include it), no seed param (re-signed per-request vocal URLs make Replicate dedup a non-issue). Implementation plan for the switch drafted; awaiting founder approval. (commit: 30026cb)

## 2026-07-04

- Investigate the arm-C mispronounced word + run two param variants (speed work follow-up). Founder verdict on the three-arm A/B: bare RVC keeps the voice identity, dryness is acceptable, but one word around 2:31–2:33 is mispronounced. Forensics (matched-sample-rate RMS per 100ms + zoomed spectrograms): the word is NOT dropped — arm C sings continuously through the window but re-articulates ~2:31.9–2:32.5 with flatter upper harmonics, a sharper cut at ~2:32.4, and a different formant re-entry than arm A. Conversion params were verified truly identical across arms from Replicate's own prediction records (index_rate 0.8 / protect 0.2 / filter_radius 4 / rms_mix_rate 0.25 / rmvpe), so the suspected mechanism is input conditioning — zsxkib de-reverbs/denoises before its internal RVC stage while the bare cog converted the raw stem. New standalone `scripts/ab-rvc-c-variants.mjs` (never imported by app code) re-ran the bare cog twice on the same hash-busted vocal: C2 protect 0.35 (18.7s compute) and C3 protect 0.35 + index_rate 0.65 (30.6s compute) — both still ~5–7× faster than the current cog; outputs `ab-listen/armC2-protect035.wav` / `armC3-protect035-index065.wav` for the founder to judge at 2:31–2:33. Staged test objects deleted after both this and the original run (the variant script cleans up automatically, success or failure). If params don't recover the word, next lever: light de-reverb/denoise on the stem before the bare cog. (commit: f81bdcd)
- Run the three-arm RVC A/B (speed work step 2) via new standalone `scripts/ab-rvc-speed.mjs` (never imported by app code; live routes untouched). Schema gate passed before spending money: `pseudoram/rvc-v2` accepts our custom model zip by URL, all four fine-tune params, rmvpe, and WAV output, with no baked-in reverb/mix stages at all. Same 3:34 lead vocal (hash-busted with a metadata-only ffmpeg remux so the current cog couldn't reuse cached preprocessing — verified: arm A's 141.6s compute matches the fresh-track median) + the MK model through all arms. Results: A (production params) 141.6s compute / B (reverb zeroed) 144.7s / **C (bare RVC) 19.2s — ~7× faster compute**, showing the MDX preprocessing + mixing is nearly all of the current cog's runtime. Honest caveat: C ate a 217s cold start (that shared model pool is colder than zsxkib's), so its wall-clock LOST this run — queue behavior is a real adoption question if C wins on quality. C outputs mono (fine for mixStems). WAVs in `ab-listen/` (gitignored) named armA/armB/armC for ear-judging; quality verdict deliberately left to the founder. One test object left in storage (`stems/ab-fresh-1783182601199-lead.wav`). (commit: 9d78a8b)
- Establish the voice-swap speed BASELINE (measurement only, no code changes) — step 1 of the speed work, from a read-only pull of the account's last 100 Replicate predictions (Jun 29–Jul 3, 93 succeeded; the planned Vercel `TIMING` capture didn't happen — no swap ran on 2026-07-04 — but the Replicate API covers every Replicate-backed stage, so nothing material was lost). Most recent complete fresh swap (2026-07-03): stem-split 120s total (0s queue / 119s compute), karaoke-split 75s (19s / 56s), RVC voice-convert 154s (14s / 140s) → ~5:10 wall-clock for the AI stages; the remainder of the observed ~7 min is un-instrumented client-side work (upload, F0 + mixStems, persist). Fleet-wide distributions confirm the 2026-07-03 investigation exactly: RVC compute is bimodal — fresh tracks 127–218s (median ~163s, n=24) vs preprocessing-cache hits 52–84s (median ~61s, n=35), plus a third tier of 3–10s for 12s fine-tune preview clips — so the cog's redundant MDX preprocessing costs ~100s per fresh conversion, the single largest avoidable cost (~30% of a fresh swap's AI compute), far exceeding queue/cold-start (usually 0–25s). Demucs is the biggest single stage (109–215s, median ~155s) but is necessary work. Baseline table recorded in PROJECT_STATUS §6. Next (approval pending): the three-arm offline A/B (current defaults / reverb-zeroed / bare-RVC `pseudoram/rvc-v2`). (investigation only — no code commit)
- Clear the two honesty-audit leftovers — the audit is now fully closed. **Finale screen (item 4's leftover):** the "📱 Share my first track" button only toasted "Opening share card for Reels…" — no share feature exists and at that point the user has no track; removed, leaving the single honest "Enter MausamVox" CTA. Fixed the same screen's false copy alongside it: the artist sub-line claimed "That was a 30-second instant clone" (nothing is cloned during onboarding since the magic-moment fix `95956a6`) → now "Next: train a clone of your voice…"; the creator sub-line claimed one-tap Reels/Shorts/WhatsApp share "is now unlocked" → now describes the real path (swap, download MP3, post anywhere); the unlock chips "1 free voice swap" and "1 express clone" named entitlements that don't exist (only the credit system does) → replaced with the verified-true "2 free previews per track" (same claim as the hero trust chips) and "3 creator tools unlocked" (Voice Swap / Voice Lab / Stem Studio). FinaleScreen's now-unused `onToast` prop removed from it and OnboardingPage. **Sidebar user row (item 3's leftover):** VSidebar + VLSidebar user rows had `cursor: pointer` with no click handler — now `cursor: default` with a comment marking the row display-only. `next build` clean. (commit: 002e180)
- Replace the false "Retrain any voice" note (honesty-audit backlog item 11 — the final numbered item; the whole 11-item audit is now closed, leaving only two small leftovers noted in PROJECT_STATUS §6). The My Voices panel footer (`VLRightPanel.tsx`) promised "Retrain any voice to improve it with new audio", but no retrain flow exists anywhere in the app (the only other "retrain" mentions in the codebase are code comments). Replaced with a tip that's true and actionable today: improve a voice by training a new clone, with a quiet room and no backing music called out as what matters most — the same dry-mic-beats-stem-separated lesson from the parked duet-swap investigation, now surfaced to users. Uses the note's existing bold styling; no layout or behavior changes. `next build` clean. (commit: a6534bd)
- Surface dashboard swap-delete failures (honesty-audit backlog item 10) — `handleDeleteSwap` in `DashboardPage.tsx` only handled `res.ok`: on a non-ok response or a thrown fetch, the × button just stopped spinning and the row stayed, with no indication anything failed. The dashboard now carries the same `VToast` state/pattern the other five pages use, and failures show a visible toast — the delete API's JSON `error` message when the server provides one, a generic "server error (status)" otherwise, and a network-error message when the fetch itself throws. The success path is untouched (optimistic row removal + authoritative `refetchCounts`). `next build` clean. (commit: b41f0ca)
- Show sidebar coming-soon items honestly (honesty-audit backlog item 9) — in both VSidebar (Voice Swap / Stem Studio) and VLSidebar (Voice Lab), the three unbuilt tools (Choir Composer, Instruments, Song Studio) and all three Library rows (My Voices, Projects, Marketplace) were styled identically to the real links but only fired a "coming soon" toast when clicked. All six rows now use the dashboard's established dim+Soon pattern: opacity 0.5, hover highlight suppressed, `cursor: default`, `aria-disabled`, and a small uppercase "Soon" pill on the right (`--soon` badge variant, dashboard colors). My Voices keeps its live clone-count badge alongside the Soon pill (the count is real; only the destination is unbuilt). The click-toast handlers are gone, which made the sidebars' `onToast` prop entirely unused — removed from both components and their three callers (VoiceSwapPage, StemStudioPage, VoiceLabPage; each still uses `showToast` for its own toasts). `next build` clean. (commit: bbc98b2)
- Fix the hero's dead "Watch Demo" button (honesty-audit backlog item 6) — the secondary CTA was a `<button>` with no onClick, promising a demo video that doesn't exist (the same no-demo-clip fact that drove the onboarding fake-player removal in `95956a6`). Replaced with a real `<a href="#features">` — the exact anchor pattern the nav links already use — relabeled "See How It Works", with the play-circle icon (which implies video) swapped for a circled down-arrow. Styling, hover effects, and layout untouched; `next build` clean. (commit: 4fff5b6)
- Remove the landing page's over-claiming copy (honesty-audit item 8, the last big one). **Hero trust badges:** "7-day money back" and "Prorated upgrades" dropped (no billing exists at all), "Quality score on every output" dropped (the fake score feature was removed weeks ago), "Live support 24/7" dropped (no support channel exists); the row now carries three claims that are true today — "Free trial — no card", "2 free previews per track", "Your voice stays private". **QualitySection:** the entire "quality confidence score" story was fabricated (big "82" score card, "score shown before every download", "free regeneration if score is below 60", "regenerate free within 10 min" — regeneration costs credits and no score exists); reworked honestly around the real quality loop — headline "You hear it before you download", checklist of free previews (first 2 per track) / A/B player / "Fine-tune your voice settings and regenerate anytime", and the score card replaced by a Fine-tune & Polish controls mock (Voice Strength/Warmth/Reverb shown as settings, not measurements). **Testimonials:** section and component deleted outright — all three quotes were fictional people, one praising the old "Gender Lock hard guarantee", another the unshipped SATB choir; an honest gap beats fabricated praise, and a code comment marks it for re-adding only with real consented user quotes. (commit: 6e4c74b)
- Build Karaoke Mode v1 ("Sing over it") — the honest option (a) from the same-day investigation: sing over a backing track, download your take; explicitly NOT a lyrics-syncing karaoke (no lyrics data exists — the panel carries a "v1 — no lyrics display yet" tag and says "lyrics display coming later"). One shared client-side `KaraokePanel` with two entry points: Stem Studio results ("Sing over it" — instrumental summed in-browser from the bass/drums/other stems via OfflineAudioContext, so no vocals) and the `/swaps/[swapId]` page ("Sing along" — backing is the saved final mix, i.e. a duet with your own cloned voice). Recording reuses Voice Lab's mime negotiation/format helpers with raw unprocessed mic capture (echo cancellation/noise suppression/AGC off — they'd duck singing against music), auto-stops when the backing ends, and shows a live timer. Afterward: play back the take, then two downloads — the raw recording (decoded to MP3 via the existing lamejs encoder, falling back to the browser's native recording format if it can't re-decode its own output) and a best-effort duet mix (take + backing offset-aligned by the measured recorder-start→playback-start gap, mixed offline, MP3-encoded). Honest limitations in the UI: headphones note (speakers bleed into the mic), and a caveat that the duet alignment is best-effort with the raw take always intact for proper mixing elsewhere. Zero backend/DB/grants. v2 deferred and logged in PROJECT_STATUS: synced lyrics, latency calibration, saving takes to the account, pitch feedback. (commit: d9a1ec1)
- Wire Express Clone up as a real, honest feature ("Quick Clone" reinterpretation from the same-day investigation — real training, not the as-marketed instant zero-shot, which would have required a second non-RVC conversion path). Express now runs the exact Studio pipeline (record → prepare-dataset → Replicate RVC train → durable model persist → usable in Voice Swap) with a lighter recipe: `/api/voice-lab/train` decides epochs server-side from the clone's DB `type` (18 express ≈ ~15 min, 50 studio ≈ ~45 min) and no longer accepts the browser-supplied `epoch` override at all. `create-clone` inserts express rows as `pending` like studio — the legacy fake path that marked them instantly `ready` with no model (the origin of unusable rows like the deleted 2Pac clone) is removed entirely. UI honesty pass in the same commit: SetupStep express card enabled with accurate copy ("Quick clone · Ready in ~15 minutes", "2–3 minutes" recording, "Standard fidelity (Studio is HD)"), TrainingStep shows the right tier name and per-tier ETA instead of "a few minutes", the impossible "Express clones are unlimited on your Pro plan" hint is replaced, and TestStep's obsolete "(full model training coming soon — currently uses your sample for voice reference)" disclaimer is deleted. Not yet validated on live: one real express train from a short sample + a swap listen is the acceptance test (weak clone identity is the known failure mode — if it's mushy, raise the express minimum recording length). (commit: ec5d4b2)
- Make the dashboard's Recent Swaps "Open" button actually open that swap (honesty-backlog item 7). New read-only page at `/swaps/[swapId]` showing the saved final mix: plays through the existing auth-gated `/api/voice-swaps/<id>/result.mp3` signing proxy with the shared AudioPlayer; details cards for voice used, created date, and duration (read live from the file's metadata — duration isn't stored in the DB); "Download MP3" implemented as fetch→blob→object-URL because the proxy's 307 to cross-origin storage makes a plain `<a download>` attribute a no-op; Delete reuses the existing endpoint then returns to the dashboard. Deliberately NOT re-editable — stems and Polish/Fine-tune settings aren't stored per swap, so a playback+download view is everything the data honestly supports (the page says so: "the finished track exactly as it was saved"). Honest empty states instead of a broken player: "Swap not found" (missing/deleted row) and "This track has expired" (row exists but the 90-day cleanup nulled its audio) with a pointer to run a new swap. `/swaps` added to the middleware redirect + matcher like the other signed-in pages. Dashboard cleanup in the same commit: "Open" links to `/swaps/<id>`, and the vestigial quality-score badge (always null since the fake score's removal) is gone along with its CSS and query column. No new backend or DB changes. (commit: 1701562)
- Fix two honesty-backlog items (2 and 5). **Credits Left staleness:** the dashboard fetched `users.credits_remaining` once on mount, so spending credits on another page/tab left a stale number until a hard refresh; the query now lives inside `refetchCounts`, so credits refresh together with the swap/clone counts on window focus, tab visibility, initial load, and after a swap delete — same no-polling listener setup, one call path. **Dead Settings items:** the avatar-dropdown "Settings" entries in the dashboard header and the landing nav were `href="#"` links that did nothing; both are now disabled (non-clickable, dimmed) with the app's standard small uppercase "Soon" marker — kept visible rather than removed, matching how unbuilt tools are shown dim+Soon on the dashboard grid. (commit: 750cf42)
- Ship real legal pages and kill all six dead footer links (honesty-audit backlog item 1). New static `/privacy` and `/terms` pages using the founder-provided Privacy Policy and Terms of Service text verbatim, rendered on a shared `LegalPage` server-component shell (`src/components/legal/LegalPage.tsx`) — dark theme, 720px readable column, logo header linking home, mini-footer cross-linking Privacy/Terms/Home. Both routes are public (middleware matcher untouched) and build as static (~767 B each). `Footer.tsx`: Privacy and Terms now link to the real pages; Support, API Docs, Discord and Twitter were removed entirely rather than left as `href="#"` anchors — each can return when its destination actually exists. (commit: 3027ffa)

## 2026-07-03

- Investigated the ~7-min voice-swap pipeline speed. Key findings, from the cog source (zsxkib/AICoverGen) + live Replicate prediction metrics: the suspected per-job 120 MB model re-download is a minor cost (~10–30s, cold instances only — warm instances already cache it, which is what exposed the wrong-voice cache-key bug fixed in `184b06f`); the real RVC-step hog is that the cog is a full song-cover pipeline running three MDX-Net separation passes (each doubled by denoise) on our already-isolated vocal before converting — predict_times are bimodal (~52–70s cache-hit re-runs vs ~137–217s fresh tracks) because it also caches preprocessing per song hash. Also found: we never zero the cog's reverb defaults, so every converted vocal ships with ~20% wet baked-in reverb. Demucs 2-stem mode evaluated and rejected (htdemucs two-stems is post-processing — no compute saved — and it breaks the 4-stem consumers); deployment keep-warm evaluated and deferred (cost vs. cold-start-only savings). Recommended next lever: swap to a bare RVC-only cog after a quality A/B. Full ranking recorded in PROJECT_STATUS §6. (investigation — code commit for the cache-key fix only: 184b06f)
- Fix a latent wrong-voice bug in RVC model caching, found during the same-day pipeline-speed investigation. The AICoverGen cog behind our pinned RVC version caches the downloaded custom model in a folder named after the download URL's last path segment (`splitext(url.split('/')[-1])`) and SKIPS the download when that folder already exists on the instance. Our model proxy URL ended in the constant `model.zip`, so every voice shared the single cache key "model" — two different voices converted back-to-back on the same warm (shared-pool) instance would make the second job silently convert with the FIRST voice's weights. Fix: voice-convert now builds the proxy URL as `/api/voice-model/<voiceId>/<voiceId>-<sha1(model_path)[:8]>.zip` — unique per voice, and the model_path hash also busts the cache after a retrain that persists a new path (relevant to the planned Raju dry-mic retrain). The proxy route itself ignores the filename segment (it signs from the DB's model_path), so only its doc comment changed; the new name is ~49 chars, far under the 255-char limit the proxy exists to dodge. Side benefit: warm-instance model caching now works CORRECTLY (legitimate skip of the ~120 MB re-download when the same voice runs again). §9 gotcha updated: never revert to a constant filename. (commit: 184b06f)
- Brand the stem-separation tech as "StemSplit Engine" in all user-facing copy instead of exposing the underlying open-source model name. A sweep for user-visible "Demucs" found three spots, all fixed: the Stem Studio page subtitle ("powered by the StemSplit Engine"), its in-progress line ("StemSplit Engine is working on …"), and the dashboard Stem Studio tool-card description. Matches the branding the landing page already uses (TechSection's "StemSplit Engine" / "StemSplit · Mausam Studio Engine"). Code comments and API internals still name Demucs/htdemucs — only user-facing strings changed. (commit: 7dce99b)
- Build Stem Studio as a real feature — new `/stem-studio` page replacing the "coming soon" placeholder, per the same-day investigation. Flow: drag/drop or pick MP3/WAV/M4A (75 MB cap, mirrors Voice Swap's validator) → presigned direct PUT to audio-uploads → `/api/stem-split` (Demucs htdemucs, existing route, already persists all 4 stems durably) → 3s poll to completion → result grid of four stem cards (Vocals 🎤 / Bass 🎸 / Drums 🥁 / Other 🎹), each with the shared AudioPlayer and a Download button through the existing auth-gated `/api/stems/download` proxy. Zero new backend routes, DB changes, or grants. Credits: 50 charged only on success via `/api/credits/deduct` action `stem_split` — the identical price the swap flow already charges internally for this operation; available on Free plan (matches the landing page's Free-tier "4-stem split" promise); `ADMIN_EMAILS` bypass; a pre-split gate blocks starting with <50 credits. Auth: `/stem-studio` added to the middleware redirect + matcher alongside voice-swap/voice-lab. Nav: dashboard tool card flipped to live, both sidebars now link to it; `VSidebar` refactored with an `activeTool` prop (default 'Voice Swap') so one sidebar serves both pages with the correct highlight. `next build` clean, route present. Deferred extras (noted in PROJECT_STATUS): combined instrumental download, zip-all, BPM/key detection, per-stem editing. (commit: 041ee22)
- Fix a credits-drain security hole in `/api/credits/deduct`, found in passing during the Stem Studio investigation: the route had NO session check — it took any `userId` from the request body and deducted with the service-role admin client (RLS bypassed), so a single unauthenticated curl could drain any user's balance. It now authenticates the session first like the app's other protected routes (`voice-swaps/delete`/`persist`, `upload-stem/presign`): 401 when not signed in, target user always derived from the session, and 403 if the body's `userId` (still sent by the sole caller, `deductCredits` in VoiceSwapPage) doesn't match the session user — no client changes needed. Also tightened amount validation (`Number.isFinite`). Left as known softness (logged in PROJECT_STATUS §3): the read-then-update deduction isn't atomic under racing requests, and swap/split charges are still client-initiated after success rather than server-gated. (commit: 1bbf49f)
- Fix the misleading dashboard "Voice Swaps: 35" stat (user saw only 4 tracks and had deleted several). Investigation confirmed the count was never stale — it's a live `count:'exact'` on `voice_swaps` and 35 rows genuinely existed; deletes DO hard-delete rows (verified live with a no-match DELETE probe). The inflation had two real sources: (1) every successful full conversion inserts a row, including every fine-tune "Apply to Full Track" / regenerate — live data showed 14 rows for one song from a single tuning session; (2) 2 phantom rows had `result_path` null (durable upload soft-failed, 1-hour Replicate URL long dead) so they counted but could never be played. Fixes: deleted the 2 phantom rows from production (both "Chaleya Jawan"/Raju, 2026-06-24); the dashboard count+list query and the voice-swap page history query now filter `.not('result_path','is',null)` so only playable swaps are counted/listed (also means the count self-corrects as the 90-day cleanup cron nulls old paths); the stat is relabeled "Voice Swaps" → "Saved Tracks" since it honestly counts saved versions, not unique songs. Also added migration `20260703000000_grant_service_role_delete_voice_swaps.sql` capturing the DELETE grant the delete route depends on — it existed only live, never in version control (grants gotcha); no new grant was needed for the filtered count (authenticated SELECT covers it). Delete-immediacy needed no change — the dashboard already drops the row optimistically and refetches the count. Not addressed (needs a product decision): the count (33) still exceeds the 4 visible rows because both lists are `.limit(4)` and no "view all swaps" surface exists (backlog item 7 territory). (commit: b3edba7)
- Fix the onboarding finale greeting every account "You're in, Mausam." (honesty-audit backlog item 4, greeting half) — `FinaleScreen.tsx` hardcoded the name in the headline. It now fetches the signed-in user itself (`supabase.auth.getUser()` in a mount effect — neither it nor its parent `OnboardingPage` had the user loaded, so there was nothing to pass down) and derives the display name exactly like the dashboard header and both sidebars: `user_metadata.full_name`/`name`, else email prefix. Until the fetch resolves (or if no user), the headline renders a plain "You're in." with no name rather than a placeholder. Left on the backlog: the screen's "Share my first track" button still only toasts. (commit: 139229a)
- Fix inconsistent user identity in the two app sidebars (honesty-audit backlog item 3) — VSidebar (Voice Swap) and VLSidebar (Voice Lab) hardcoded "Mausam", avatar "M", and "Pro Plan" for every account, while the dashboard header already showed the real derived name. Both sidebars now derive the display name + initial exactly like the dashboard (`user_metadata.full_name`/`name`, else email prefix) inside the `supabase.auth.getUser()` call they already made for the My Voices badge, and show the real `users.plan` formatted as "Free/Starter/Pro/Studio Plan". Plumbing matches each page's existing data flow: VoiceSwapPage already fetched `plan` alongside credits, so VSidebar takes it as a new `plan` prop; VLSidebar owns its own users query, so `plan` was added to its existing select. Placeholders ("…" name/plan, "·" avatar) render until data loads. Left as-is (still on the audit backlog): the user row's cursor-pointer with no click handler. (commit: 68d7478)
- Remove the fake Quality Score panel from the swap Result screen — the #2 finding of the app-wide honesty audit. Every swap showed the same hardcoded numbers (ScoreRing "~82/100" + four invented sub-score bars: Voice Match 94 / Pitch Accuracy 87 / Naturalness 78 / Emotion Transfer 71) with nothing ever computed or stored (`persist` writes `quality_score: null`; the "Est." tag didn't make a constant an estimate). Replaced with a "Swap complete" summary showing only real facts: the voice model(s) used — new `voiceName` prop passed from VoiceSwapPage (duet-aware: joins both names when a second converted vocal exists), the track duration read from the loaded audio metadata, and whether the file is the full mix (vocals + music) or converted vocals only. Removed `SCORE_BARS`, the `ScoreRing` component, the `barsAnimated` mount effect, and the `.vs-est-tag` style; added a gradient check-circle + reused the existing chip style. Dashboard/RightPanel score badges were already honest (render only when `quality_score` is non-null, which never happens). (commit: 98bed97)
- Remove the fake demo player from the onboarding "magic moment" screen — the #1 finding of the app-wide honesty audit (most misleading interaction in the app: a new user believed they'd heard a real AI demo). The old screen had a play button that toasted "Playing your track…", animated canvas waveform bars for 11 s, showed a fake "0:30" duration and a fake "87" score badge — with **no audio element anywhere in the file** — plus a "Try another style" button that only toasted "Regenerating with different settings…". No shippable demo clip exists (repro-listen/ is untracked local test scratch), so the screen was redesigned as honest static content rather than wiring a clip: the result card is now explicitly badged "PREVIEW", keeps the track/voice mock rows, and renders a static decorative waveform (deterministic bar heights, no controls, no duration, no score); persona sub-copy reworded to describe what the user WILL get instead of claiming a swap already happened ("We swapped the vocals…") or citing unbuilt capabilities (licensed/cleared voice library, 9:16 export); single honest CTA "Let's make mine →". `resultPlaying` state and the screen's unused `onToast` prop removed from OnboardingPage. (commit: 95956a6)
- Replace the Polish panel's three sliders (Warmth/Reverb/Echo) with rotary KNOBS, implementing the design agreed in the 2026-07-02 investigation. New from-scratch `PolishKnob` component in `ResultStep.tsx`: 48px SVG dial with a 270° sweep (gap at the bottom), #1E1E3A track arc + #8B5CF6→#EC4899 gradient value arc (strokeDasharray technique, per the ScoreRing precedent), rotating #C4B5FD pointer line, and label + formatted value readout below (same 'Off'/'+x.x dB'/'x% wet' strings as before). Interaction: vertical pointer-drag with pointer capture (~200px = full 0–100 travel, `touch-action:none` so mobile drags don't scroll), keyboard a11y via `role="slider"` + aria-value* (arrows ±1, PageUp/Down ±10, Home/End), and double-click to reset to 0; the per-row hint text moved into each knob's `title` tooltip (which also mentions the drag/reset gestures). All three knobs sit in one centered row, so the panel is more compact and fits single-row on mobile (old stacked-slider media query removed). Pure UI swap as designed: knobs drive the exact same `warmth`/`reverb`/`echo` 0–100 state via the same setters — zero changes to mixStems, the 280ms debounce, the settle-gated persist, or any audio code, so the both-tabs + baked-into-saved-track guarantees carry over untouched. No backend/deps; `next build` clean. (commit: e8cccf5)

## 2026-07-02

- Investigated redesigning the Polish panel's three sliders (Warmth/Reverb/Echo) as rotary KNOBS — confirmed feasible as a pure UI swap. No knob/dial component exists anywhere in the codebase (no UI libraries at all — every control is a hand-rolled native element), so it's a from-scratch `PolishKnob` component; the in-file `ScoreRing` SVG (arc via strokeDasharray + the #8B5CF6→#EC4899 gradient) is the rendering precedent. The sliders' only contract with the audio engine is `value` + `onChange(0–100)`, so knobs drive the exact same warmth/reverb/echo state with zero changes to mixStems, debounce, or persist. Agreed design: 48px SVG dial, 270° sweep, #1E1E3A track + gradient value arc, rotating #C4B5FD pointer, label + value readout below, vertical-drag interaction (~200px full travel) with arrow-key a11y (`role="slider"`) and double-click-to-0; all three knobs in one centered row (panel footprint unchanged, works single-row on mobile, hints move to title tooltips). Scope: ~130–160 line diff, all in `ResultStep.tsx`; no backend/deps. Not yet implemented. (investigation only — no code commit)
- Add **Echo** — the third effect in the vocal polish layer, chained after Reverb per the delay investigation (`ResultStep.tsx` only, no backend). `mixStems()` gained `{ echo }` and a lazily-built shared feedback-delay bus: `input → dry(1−wet) → destination` in parallel with `input → DelayNode(0.30s) → wet → destination`, plus a feedback loop `delay → lowpass(3.5kHz) → feedbackGain(0.35) → delay` so each successive repeat gets darker (tape-echo) and dies after 2–3 audible repeats (35% → 12% → 4%). Delay time and feedback are fixed internal constants (one-knob philosophy, like Reverb's fixed IR); the slider maps 0–100 → 0–50% wet (`ECHO_MAX_WET = 0.5`, same ceiling as Reverb). Chaining is via a new `getVocalSink()` indirection: the vocal path (and Reverb's dry/wet outputs) terminate at the echo bus input when echo>0, else directly at the destination — so at echo=0 zero nodes are created and the render is byte-identical to before (matching Warmth/Reverb's guarantee); the music bed never routes through it. Echo threads through every existing polish touch-point: debounced `echo`/`debouncedEcho`/`echoRef` state, all three `mixStems` call sites (full-mix build, live polish re-render, vocals-only blend), the vocals-only raw-URL fast-path condition, and the persist-settle guard — so it applies on BOTH tabs and bakes into the saved Recent Swaps file, with the same known one-shot-persist limit as Warmth/Reverb. New "Echo — adds repeats/echo to the vocal" slider row in the Polish panel (reuses existing styles; no CSS changes). Known limit shared with Reverb: repeats past the end of the render are truncated (render length = longest stem). (commit: 0a9cd27)
- Raise the Reverb polish control's maximum wet mix from 35% to 50% (`REVERB_MAX_WET` 0.35 → 0.5 in `ResultStep.tsx`). Single-constant change — the slider's 0–100 range now maps to 0–50% wet, giving more headroom at the top; behavior otherwise identical (reverb=0 still inserts no nodes). (commit: 9a5581d)
- Fix the "fresh Full-song swap plays the WRONG voice (different each attempt) and is missing music" bug — root-caused to the `mvox_stem_session` localStorage cache restoring **silently on every page mount** (UploadStep auto-flips to "done", so a restored track looks like a fresh swap) combined with only the full vocal stem having a durable re-signable path. Two failure legs: (1) the lead vocal — the DEFAULT input to every single-voice swap via karaoke-split — carried no `vocalsPath`, so voice-convert's existing re-sign safety net never fired and a stale cached URL went straight to RVC (wrong/garbage voice; per-attempt variance came from flaky client-side F0 detection against the same dying link feeding different auto pitch-shifts into an otherwise deterministic RVC); (2) the music stems (bass/drums/other) had no durable paths at all and `mixStems`' `fetchDecode` swallowed failures, so expired music URLs produced a vocals-only mix that was marked "ready" and persisted as the full song. Fix, mirroring the existing `persistVocals` pattern: new shared `lib/stem-persist.ts` helper; stem-split now persists **all four** Demucs stems and returns `bassPath`/`drumsPath`/`otherPath`; karaoke-split persists lead/backing (`stems/<id>-lead.mp3`) and returns their paths (`maxDuration` 30→60); gender-split returns the `gender-split/<hash>-{male,female}.mp3` paths its `persistStems` already created; new auth-gated `/api/stems/refresh` batch re-signs stem paths (prefix-validated: `stems/`, `gender-split/`, `<uid>/`); `StemResult` carries all seven new path fields through the cache; the restore effect re-signs every stem URL via the new route **before** using the result and shows a visible "Restored …" toast; `handleProcess` sends the durable path matching whichever stem is actually converted (lead/male/female/full — single-job and dual-job branches) so the server-side re-sign now fires on every swap; and `mixStems` is strict — any failed stem fetch fails the whole mix into the visible 'error' state (auto-switching the player to Vocals-only) instead of silently shipping a partial mix. 5h cache TTL kept: re-signing on restore makes it safe. (commit: 277cae1)

## 2026-07-01

- Investigated two reported Warmth-control bugs ("slider does nothing" / "preview doesn't match saved file") — root-caused to the Polish panel being shown on both the Full-song and Vocals-only tabs while warmth was only ever wired into the Full-song `mixStems()` call. Vocals-only played `convertedVocalsUrl` directly for standard swaps, or a `mixStems()` blend built with no `{warmth}` opt for duet — so soloing the vocal (the natural way to judge an EQ tweak) produced zero change, and the saved file (always built from the Full-song render) differed from what had just been previewed. Fixed: the vocals-only blend effect now passes `debouncedWarmth` into `mixStems()` and depends on it (for both duet N>1 and single-vocal swaps), falling back to the instant raw `convertedVocalsUrl` only when there's nothing to blend or warm (single vocal, warmth=0). Also raised `WARMTH_MAX_DB` 6→10 dB so the low-shelf boost is clearly hearable instead of getting masked by the instrumental bed; warmth=0 still inserts no filter node (byte-identical). Found and deliberately left alone: an unrelated, currently-unreachable gap in the duet Mode 2/3 female-vocal RVC job (missing `protect`/`filterRadius`/`rmsMixRate` overrides) — logged in PROJECT_STATUS.md §6 for whenever duet fine-tuning ships. (commit: f0408c1)
- Honesty pass on the Configure/swap screen (`ConfigStep.tsx`) after auditing which of its six controls actually reach the backend — confirmed only Style Intensity (`index_rate`) and Pitch Shift are real; Age Range, Accent, and Output Language are never sent to any API. Rather than remove them (keeps the product vision visible), disabled all three (native `disabled` on the SegControl buttons / `<select>`s, greyed via CSS) and added a "Coming soon" badge next to each label. Separately, Gender Lock's status copy claimed a "hard guarantee" implying it constrains the *output voice's* gender — it doesn't; its only real effect is routing which duet stem gets converted, and only in single-singer duet mode. Reworded both status states (duet-split and no-split) to describe that actual behavior instead of an unmet guarantee; left the control itself enabled since the routing effect is real. Goal: nothing on Configure implies a capability the pipeline doesn't deliver. (commit: 5dbac6e)
- Add Reverb — the second effect in the vocal polish layer, chained after Warmth per the earlier investigation's design. `audioClip.ts` gained `createReverbImpulse()`, a synthetic exponentially-decaying noise buffer (~1.8s) generated in code — no bundled IR asset or network fetch. `mixStems()`'s vocal chain is now `gain → [warmth EQ if warm] → dry/wet split → destination`; the dry/wet split uses one shared `ConvolverNode` + dry/wet `GainNode` bus per call, fed by every vocal channel (convolution distributes over summed inputs, so sharing one convolver across duet channels is identical to giving each its own, just cheaper). reverb 0–100 maps to 0–35% wet — capped well under 100% since a fully-wet soloed vocal sounds washy/unnatural. At reverb=0 no convolver/split nodes are created at all, matching Warmth's byte-identical-at-zero guarantee. New debounced `reverb`/`debouncedReverb` state mirrors warmth's pattern exactly (280ms debounce, ref for the mount-time build, settle-gated persist); the warmth re-render effect, the vocals-only blend effect, and the persist-settle guard were all generalized to also key off reverb, so it gets the same both-tabs-plus-saved-file guarantee Warmth already has. New "Reverb" slider added next to Warmth in the existing Polish panel. No backend/API/dependency changes. (commit: 5c35648)

## 2026-06-30

- Recover clone-vocal quality — switch RVC `output_format` from `mp3` to `wav` in voice-convert (`route.ts:262`). The vocal was double-lossy-encoded (Demucs separation mp3 → RVC mp3) while the instrumental encodes only once, making the clone vocal sound duller than the music (~75% vs 95%). WAV removes that second compression; downstream (mixStems, Fine-tune preview) already decodes WAV. Single-variable change to measure impact alone — Demucs format untouched. Tradeoff: WAV is ~10× larger, so the vocal fetch in the browser mix/preview is slower. (commit: e5968d7)
- Add **Warmth** vocal-polish control to the Voice Swap result (first step of the polish/effects layer) — `ResultStep.tsx` only, no backend. `mixStems` gained an optional `{ warmth }`: the converted-vocal channel now routes through a Web Audio low-shelf BiquadFilter (200Hz, warmth 0–100 → 0–6 dB); the music bed is never filtered and warmth 0 inserts no node (mix byte-identical to before). New "Polish" UI block above the Fine-tune panel with one debounced (280ms) "Warmth" slider; a dedicated effect re-renders only the swapped full-song mix on change (no `fullMixState` flip, player stays usable). Because the parent persists exactly once (nulls its persist context after the first `onFullMixReady`), the single upload is now **deferred until warmth settles** (mix ready + not mid-drag + not re-rendering, +1s) so the saved Recent Swaps file reflects the chosen warmth. Known limit: re-adjusting warmth after that one settle updates the live preview but not the already-saved file (one-shot parent persist). (commit: 9b0100a)
- Fix Voice Lab Record-step layout regressions from the read-aloud script card: (1) record/stop button was clipped — `.vlrec-card` had no `flex-shrink`, so the flex-column workspace compressed it and its `overflow:hidden` clipped the bottom instead of scrolling; added `flex-shrink:0` (card keeps full height, workspace scrolls) and compacted the script card (smaller padding/font/line-height) so timer + button stay visible without scrolling. (2) Removed the now-duplicate instruction line in both panels (Quick "Speak naturally…" + Pro "Pick your audio interface…") since the script card replaces it; dropped their dead `.qr-hint`/`.pr-hint` styles. Timer, device label, and waveform/level meter untouched. (commit: b7887ed)
- Add a read-aloud script to the Voice Lab recording UI — users previously had no guidance on what to say while recording a clone. New `recordingScripts.ts` holds 3 phonetically-varied passages (~30–45s each, so one pass clears the 30s minimum) + a `nextScript()` rotor; RecordStep renders the active passage above the Quick/Pro capture panel (not Upload) under the label "Read this aloud (or sing across your range):" with a "New script" shuffle button. Includes explicit singing guidance (go low/high + soft/loud, don't read flatly) since clones here are often used for singing. Display-only — no backend/training changes. (commit: 61e7ad2)
- Fix marketing copy that over-promised 6-stem separation — we ship 4-stem htdemucs (guitar+piano/6-stem deferred). Corrected three landing spots: Smart Stem Studio card desc + badge (Features.tsx) "6-stem"/"6 Stems" → "4-stem"/"4 Stems"; StemSplit Engine desc (TechSection.tsx) "Clean 6-stem separation" → "Clean 4-stem separation"; Starter pricing tier (Pricing.tsx) "6-stem split + BPM/key" → "4-stem split + BPM/key". Left the unrelated Choir "SATB · 4 Stems" copy, the already-accurate Free-tier "4-stem split", and dashboard copy untouched. (commit: 76a0164)
- Fix silently-ignored Demucs param in stem-split — the cjwbw/demucs cog's stem-selection key is `model_name`, not `model`, so our `model: 'htdemucs'` was stripped by Replicate and the cog ran its default htdemucs (4-stem) by luck. Renamed to `model_name: 'htdemucs'` to make the intent explicit/correct, and dropped the equally-ignored `mp3: true` key (output_format already defaults to mp3). Output is identical — still 4-stem htdemucs, no behavior change. (Investigation also confirmed we are NOT paying 6-stem compute: the cog's output object always has 6 fixed keys, but guitar/piano come back empty under htdemucs.) (commit: 401a5fe)
- Add per-stage cold-start vs compute timing logs to the pipeline routes (instrumentation only, no behavior change). New `logReplicateTiming` helper (`src/lib/replicate-timing.ts`) logs one `TIMING` line per finished prediction — cold-start/queue (started_at−created_at), compute (metrics.predict_time), total — wired into stem-split/Demucs, karaoke-split, and voice-convert/RVC GET success branches. gender-split (MVSEP) logs wall-clock total from the client-supplied elapsedMs (MVSEP returns no queue/compute split). Read in Vercel logs by searching `TIMING` after one real swap. (commit: 73fb83c)
- Recent Swaps now saves the FULL track (clone vocal + instrumental) instead of the vocal-only file — ResultStep uploads the built mix (presign→PUT) and persist stores it as result_path; falls back to the vocal if the mix/upload fails; previews never upload. (commit: ff33ce5)
- Fix hardcoded sidebar "My Voices" badge — VSidebar + VLSidebar now render a live voice_clones count (count:exact, head, eq user_id) instead of a static '3'. (commit: 42b21ba)
- Fix stale dashboard counts — Voice Swaps + Voice Clones counts now refetch on window focus and tab visibilitychange (were fetched once on mount); delete handler refetches authoritatively instead of optimistic local −1. (commit: 3bb8d75)
- Restore duet-split warning before auto-split — declared-duet uploads no longer silently run the 250cr gender-split; the existing amber gate renders and the user must click "Split duet · 250 cr · Premium" to proceed (Option A: handleStemDone no longer auto-calls runGenderSplit / sets genderSplitting). (commit: d273e69)

## 2026-06-29

- Duet swap "male sounds like original" investigation — root-caused to **weak clone identity from stem-separated training**. Ruled out: the four RVC conversion params (index_rate/protect/filter_radius/rms_mix_rate identical on single-voice and duet paths), pitch (male stem already within Raju's ~235 Hz range; auto key-match skipped on duet stems), routing, and mix wiring. Dropped undertraining/samplerate as the cause. Agreed fix: clean dry-mic retrain of Raju (also fixes glitchy non-duet clone quality — same root cause). Parked pending retrain. (investigation only — no code commit)
- Add start-point picker to Fine-tune preview — choose which 12s window of the song to preview (skip music-only intros); trimAudioToClip gains startSeconds, clip cache key includes start+length, control bounded by song duration. (commit: eeb0fa1)
- Shorten Fine-tune preview clip from 30s to 12s (PREVIEW_CLIP_SECONDS=12 + all "30 sec" UI text/labels updated); faster + cheaper tuning previews. (commit: 8f37e83)
- Add "Reset to defaults" button to the Fine-tune Advanced panel — restores all tuner sliders (Voice strength/index_rate, protect, filter_radius, rms_mix_rate) to defaults in one click. (commit: 213aa49)
- Add "Known gotchas (reference, do not re-debug)" section to PROJECT_STATUS.md (MVSEP label reliability, Replicate filename/proxy fix, lamejs fork, undici allowH2:false, Node 26 HTTP/2, Supabase Pro for 116MB+ models). (commit: 40c4de2)
- Add handoff files: PROJECT_STATUS.md, CHANGELOG.md, CLAUDE.md (working rules + gotchas + session handoff protocol), AGENTS.md. (commit: 4da1ebc)
